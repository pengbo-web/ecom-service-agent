"""记忆提取 Prompt：短期记忆 (STM) 和长期记忆 (LTM) 的事实抽取。"""

STM_EXTRACTION_PROMPT = """你是对话分析助手。基于下面的电商客服对话片段，提取用户的关键信息和偏好。

已有的用户信息（如果有的话，避免重复，可以更新或补充）：
{existing_facts}

请从对话中提取以下类型的信息（如果对话中出现）：
1. 用户身份信息：姓名、会员等级、联系方式
2. 用户偏好：喜欢的颜色、风格、品牌、价格区间
3. 关注的商品或品类
4. 用户情绪/态度：满意、不满、焦虑、着急等
5. 特殊需求：送货时间、地址偏好、礼品包装等
6. 其他值得记住的信息

输出要求：
- 每条信息一行，用简洁中文描述
- 只提取对话中明确提及或可以合理推断的信息
- 如果对话中没有新的有用信息，输出「无新信息」
- 不要输出编号、不要加多余的解释"""


LTM_EXTRACTION_PROMPT = """你是用户画像分析助手。基于以下电商客服对话（可能包含对话摘要），提取值得长期记忆的用户信息。

这些信息将在未来的对话中帮助客服更好地服务该用户。

用户已有的长期记忆（避免重复）：
{existing_ltm}

请输出 JSON 格式，包含两个字段：
1. "facts": 值得长期记忆的用户信息数组，每条包含：
   - "content": 信息内容（简洁中文）
   - "category": 分类（identity/preference/behavior/issue/other）
2. "interaction_summary": 本次交互的一句话摘要

示例输出：
{{
  "facts": [
    {{"content": "用户是钻石会员", "category": "identity"}},
    {{"content": "用户偏好红色系的运动鞋", "category": "preference"}}
  ],
  "interaction_summary": "用户咨询了Nike运动鞋的退换货，最终申请了退款"
}}

如果对话中没有值得长期记忆的新信息，facts 返回空数组。
interaction_summary 必须填写。

只输出 JSON，不要加其他内容。"""


LTM_CURATION_PROMPT = """你是用户长期记忆的策展员。下面是关于同一位用户的一批长期记忆事实（含新旧混合）。
请把它整理成一份更干净的清单，规则如下：

1. **合并近义**：语义重复或高度重叠的多条，合并成一条更清晰的表述（例如「喜欢红色」「偏好红色款」→「偏好红色系商品」）。
2. **就地纠正**：若两条相互矛盾，只保留更新/更正后的那条，丢弃过时的。
3. **淘汰琐碎**：一次性、已解决的临时问题不必长期保留。
4. **重要性优先**：最多保留 {max_facts} 条；超出时按重要性取舍——身份信息 > 稳定偏好 > 行为习惯 > 未决问题，优先丢弃最不重要的。
5. **不得杜撰**：只能对给定事实做保留/合并/改写，绝不新增清单里没有的信息。

待整理的事实清单：
{facts}

只输出 JSON，格式如下，不要加 markdown 代码块或任何解释：
{{"facts": [{{"content": "简洁中文", "category": "identity|preference|behavior|issue|other"}}]}}"""
