openai>=1.0.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0
mcp>=1.8.0
chromadb>=0.5.0
fastapi>=0.110.0
uvicorn>=0.29.0
httpx>=0.27.0
pyyaml>=6.0        # Skill frontmatter 解析（yaml.safe_load，支持嵌套/列表/多行）
redis>=5.0         # 会话存储 Redis 后端（生产/多实例热会话）
fakeredis>=2.0     # 测试用内存假 Redis（离线单测,不触网）

# 观测平台体验(可选):Langfuse 自托管 drop-in 上报,langfuse_enabled=True 时需要
langfuse>=3.0.0

# 协作队列 PG 载体(PG2,可选):collab_bus_backend=pg 时需要。
# [binary] 带预编译轮子,免装 libpq 与编译器。默认后端仍是 sqlite,不装它也能跑
# (工厂会回落 SQLite 并记 warning),所以这条不是硬依赖。
psycopg[binary]>=3.1
