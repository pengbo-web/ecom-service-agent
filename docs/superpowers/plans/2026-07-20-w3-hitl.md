# W3（下）：人机协作 Human-in-the-Loop Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 给服务加人机协作闭环：命中升级条件（低置信度 / 模型判定转人工 / 敏感意图）时自动生成**交接上下文包**并推入**坐席队列**；提供坐席面板（查看待接管、一键解决）与 **Xianyu 式人工接管开关**（会话切人工/自动，带超时自动回落）。人工接管中的会话直接由真人处理，Agent 不自动回复。

**Architecture:** **全部在服务层挂载，`chat.py` 零改动**。新增 `app/hitl/` 模块：`escalation`（纯函数判定）、`HandoffQueue`（SQLite 队列）、`ManualMode`（内存态 + 超时）、`HitlManager`（编排）。`run_agent_streaming` 新增可选 `hitl` 参数：正常回复产出后按 `result` 判定升级，命中则推队列并发 `handoff` 事件。人工接管在 `/api/chat` 端点前置判断：命中则短路返回"人工处理中"，不调用 Agent。升级事件并入 W2 Trace（`hitl` span）。

**Tech Stack:** Python 3.11+、`sqlite3`、dataclass、FastAPI、pytest（离线，用 fake result/agent + 注入时钟）。

## Global Constraints

- **Python 3.11+**；解释器 `D:/2026项目/ecom-service-agent/.venv/Scripts/python.exe`。
- **`chat.py`/`orchestrator.py` 核心零改动**：HITL 只出现在 `app/hitl/` 与服务层。
- **可开关**：`settings.hitl_enabled`（默认 True）；关闭时 `run_agent_streaming` 行为与 W3-护栏一致。
- **不破坏 W1/W2/W3-护栏**：`run_agent_streaming` 新增 `hitl` 默认 None；既有测试须继续通过。
- **测试离线**：升级判定是纯函数；队列用临时 SQLite；人工接管注入假时钟测超时。不依赖 LLM/网络。
- **YAGNI**：坐席"接管"只做"判定 + 交接上下文 + 队列可视化 + 人工模式开关"，不做真人实时聊天系统（与设计文档第 2 节③一致）。
- **Windows 友好**：`pathlib`。

## 事件与数据约定（在既有事件基础上新增）

- `{"type": "handoff", "reasons": [<str>...], "handoff_id": <str>}`（升级为转人工时，在 metadata 之后发出）
- Trace 中升级记为 `Span(kind="hitl", name="handoff", meta={reasons})`。
- 人工接管短路时的回复事件仍是标准 `reply` + `done`。

---

## File Structure

- `app/config/settings.py` — 修改：新增 `hitl_enabled` / `hitl_confidence_threshold` / `hitl_db_path` / `manual_mode_timeout`。
- `app/hitl/__init__.py` — 新建：导出 `HandoffQueue` / `ManualMode` / `HitlManager` / `should_escalate` / `build_handoff_bundle`。
- `app/hitl/escalation.py` — 新建：`should_escalate`、`SENSITIVE_INTENTS`。
- `app/hitl/handoff.py` — 新建：`build_handoff_bundle`。
- `app/hitl/queue.py` — 新建：`HandoffQueue`（SQLite）。
- `app/hitl/manual_mode.py` — 新建：`ManualMode`（内存 + 超时）。
- `app/hitl/manager.py` — 新建：`HitlManager`（编排 evaluate/escalate + 持有 queue/manual_mode）。
- `app/api/streaming.py` — 修改：接入 `hitl`（升级判定 → handoff 事件 + 入队）。
- `app/observability/tracer.py` — 修改：`on_event` 支持 `handoff` 事件记 span。
- `app/observability/metrics.py` — 修改：新增 `handoffs` / `escalation_rate`。
- `app/api/app.py` — 修改：装配 HitlManager；人工接管短路；新增 handoff/takeover 端点。
- `web/chat.html` — 修改：新增「🎧 坐席」标签 + 人工接管开关 + handoff 事件展示 + 看板转人工卡片。
- `tests/test_hitl_escalation.py` / `test_hitl_queue.py` / `test_hitl_manual.py` / `test_streaming_hitl.py` / `test_hitl_api.py` — 新建。

---

## Task 1: 升级判定 escalation + settings

**Files:**
- Modify: `app/config/settings.py`
- Create: `app/hitl/escalation.py`
- Create: `app/hitl/handoff.py`
- Create: `app/hitl/__init__.py`（先导出 escalation + handoff）
- Test: `tests/test_hitl_escalation.py`

**Interfaces:**
- Produces:
  - `settings.hitl_enabled: bool`、`hitl_confidence_threshold: float=0.6`、`hitl_db_path: str`、`manual_mode_timeout: int=3600`
  - `escalation.py`：`SENSITIVE_INTENTS: set[str]`（默认 `{"complaint"}`）；`should_escalate(intent:str, confidence:float, requires_human:bool, threshold:float, sensitive_intents:set)->list[str]`（返回命中原因列表，空列表=不升级）
  - `handoff.py`：`build_handoff_bundle(session_id, user_input, reply, intent, confidence, reasons, recent_context=None)->dict`

- [ ] **Step 1: 写失败测试**

`tests/test_hitl_escalation.py`：
```python
from app.hitl.escalation import should_escalate, SENSITIVE_INTENTS
from app.hitl.handoff import build_handoff_bundle


def test_escalate_on_requires_human():
    r = should_escalate("order_query", 0.95, True, 0.6, SENSITIVE_INTENTS)
    assert r and any("转人工" in x for x in r)


def test_escalate_on_low_confidence():
    r = should_escalate("order_query", 0.4, False, 0.6, SENSITIVE_INTENTS)
    assert r and any("置信度" in x for x in r)


def test_escalate_on_sensitive_intent():
    r = should_escalate("complaint", 0.95, False, 0.6, {"complaint"})
    assert r and any("敏感意图" in x for x in r)


def test_no_escalation_for_normal():
    assert should_escalate("order_query", 0.95, False, 0.6, SENSITIVE_INTENTS) == []


def test_bundle_shape():
    b = build_handoff_bundle("s1", "退款啊", "正在处理", "complaint", 0.5, ["敏感意图(complaint)"])
    assert b["session_id"] == "s1"
    assert b["intent"] == "complaint"
    assert b["reasons"] == ["敏感意图(complaint)"]
    assert "user_input" in b and "reply" in b
```

- [ ] **Step 2: 运行确认失败**

Run: `cd "D:/2026项目/ecom-service-agent" && .venv/Scripts/python.exe -m pytest tests/test_hitl_escalation.py -v`
Expected: FAIL —— `ModuleNotFoundError: No module named 'app.hitl'`。

- [ ] **Step 3: 实现**

`app/config/settings.py` 在护栏配置块下方加：
```python
    # 人机协作 HITL（W3）
    hitl_enabled: bool = True
    hitl_confidence_threshold: float = 0.6
    hitl_db_path: str = "app/sessions/hitl.db"
    manual_mode_timeout: int = 3600   # 人工接管超时（秒），超时自动回落自动模式
```
`app/hitl/escalation.py`：
```python
"""转人工升级判定（纯函数）。"""

# 敏感意图：命中即建议转人工（可按业务调整）
SENSITIVE_INTENTS = {"complaint"}


def should_escalate(intent: str, confidence: float, requires_human: bool,
                    threshold: float, sensitive_intents: set) -> list:
    """返回命中的升级原因列表；空列表表示无需转人工。"""
    reasons = []
    if requires_human:
        reasons.append("模型判定需转人工")
    if confidence < threshold:
        reasons.append(f"置信度过低({confidence:.2f} < {threshold})")
    if intent in sensitive_intents:
        reasons.append(f"敏感意图({intent})")
    return reasons
```
`app/hitl/handoff.py`：
```python
"""交接上下文包构建。"""

from typing import Optional


def build_handoff_bundle(session_id: str, user_input: str, reply: str,
                         intent: str, confidence: float, reasons: list,
                         recent_context: Optional[list] = None) -> dict:
    return {
        "session_id": session_id,
        "user_input": user_input,
        "reply": reply,
        "intent": intent,
        "confidence": confidence,
        "reasons": reasons,
        "recent_context": recent_context or [],
    }
```
`app/hitl/__init__.py`：
```python
from app.hitl.escalation import should_escalate, SENSITIVE_INTENTS
from app.hitl.handoff import build_handoff_bundle

__all__ = ["should_escalate", "SENSITIVE_INTENTS", "build_handoff_bundle"]
```

- [ ] **Step 4: 运行确认通过**

Run: `cd "D:/2026项目/ecom-service-agent" && .venv/Scripts/python.exe -m pytest tests/test_hitl_escalation.py -v`
Expected: PASS（5 passed）。

- [ ] **Step 5: 提交**

```bash
git add app/config/settings.py app/hitl/escalation.py app/hitl/handoff.py app/hitl/__init__.py tests/test_hitl_escalation.py
git commit -m "feat(hitl): 转人工升级判定 + 交接上下文包"
```

---

## Task 2: HandoffQueue（SQLite 坐席队列）

**Files:**
- Create: `app/hitl/queue.py`
- Modify: `app/hitl/__init__.py`
- Test: `tests/test_hitl_queue.py`

**Interfaces:**
- Produces: `class HandoffQueue(db_path=None, id_factory=None)`：
  - `init_schema()`
  - `add(bundle: dict) -> str`（返回 handoff_id；status="pending"）
  - `list_pending() -> list[dict]`
  - `get(handoff_id) -> Optional[dict]`
  - `resolve(handoff_id) -> bool`（置 status="resolved"）
  - `count_pending() -> int`

- [ ] **Step 1: 写失败测试**

`tests/test_hitl_queue.py`：
```python
import itertools
from app.hitl.queue import HandoffQueue


def _q(tmp_path):
    ids = itertools.count(1)
    q = HandoffQueue(str(tmp_path / "h.db"), id_factory=lambda: f"h{next(ids)}")
    q.init_schema()
    return q


def test_add_and_list_pending(tmp_path):
    q = _q(tmp_path)
    hid = q.add({"session_id": "s1", "intent": "complaint", "reasons": ["x"]})
    assert hid == "h1"
    pending = q.list_pending()
    assert len(pending) == 1
    assert pending[0]["session_id"] == "s1"
    assert pending[0]["status"] == "pending"
    assert pending[0]["intent"] == "complaint"


def test_resolve(tmp_path):
    q = _q(tmp_path)
    hid = q.add({"session_id": "s1", "reasons": []})
    assert q.resolve(hid) is True
    assert q.count_pending() == 0
    assert q.list_pending() == []


def test_resolve_missing(tmp_path):
    q = _q(tmp_path)
    assert q.resolve("nope") is False


def test_get_returns_bundle(tmp_path):
    q = _q(tmp_path)
    hid = q.add({"session_id": "s2", "user_input": "退款", "reasons": ["敏感意图"]})
    got = q.get(hid)
    assert got["session_id"] == "s2"
    assert got["payload"]["user_input"] == "退款"
```

- [ ] **Step 2: 运行确认失败**

Run: `cd "D:/2026项目/ecom-service-agent" && .venv/Scripts/python.exe -m pytest tests/test_hitl_queue.py -v`
Expected: FAIL —— `ModuleNotFoundError: No module named 'app.hitl.queue'`。

- [ ] **Step 3: 实现**

`app/hitl/queue.py`：
```python
"""坐席队列（SQLite 持久化）。"""

import json
import sqlite3
import uuid
from pathlib import Path
from typing import Optional

from app.config.settings import settings


class HandoffQueue:
    def __init__(self, db_path: Optional[str] = None, id_factory=None):
        self.db_path = db_path or settings.hitl_db_path
        self._id = id_factory or (lambda: uuid.uuid4().hex[:12])

    def connect(self) -> sqlite3.Connection:
        Path(self.db_path).parent.mkdir(parents=True, exist_ok=True)
        conn = sqlite3.connect(self.db_path)
        conn.row_factory = sqlite3.Row
        return conn

    def init_schema(self) -> None:
        conn = self.connect()
        try:
            conn.executescript(
                """
                CREATE TABLE IF NOT EXISTS handoffs (
                    handoff_id TEXT PRIMARY KEY,
                    session_id TEXT,
                    intent TEXT,
                    reasons TEXT,
                    status TEXT DEFAULT 'pending',
                    created_at TEXT DEFAULT CURRENT_TIMESTAMP,
                    resolved_at TEXT,
                    payload TEXT
                );
                CREATE INDEX IF NOT EXISTS idx_handoff_status ON handoffs(status);
                """
            )
            conn.commit()
        finally:
            conn.close()

    def add(self, bundle: dict) -> str:
        hid = self._id()
        conn = self.connect()
        try:
            conn.execute(
                "INSERT INTO handoffs (handoff_id, session_id, intent, reasons, payload) "
                "VALUES (?, ?, ?, ?, ?)",
                (hid, bundle.get("session_id"), bundle.get("intent"),
                 json.dumps(bundle.get("reasons", []), ensure_ascii=False),
                 json.dumps(bundle, ensure_ascii=False)),
            )
            conn.commit()
        finally:
            conn.close()
        return hid

    def _row(self, r) -> dict:
        d = dict(r)
        d["reasons"] = json.loads(d["reasons"]) if d.get("reasons") else []
        d["payload"] = json.loads(d["payload"]) if d.get("payload") else {}
        return d

    def list_pending(self) -> list:
        conn = self.connect()
        try:
            rows = conn.execute(
                "SELECT * FROM handoffs WHERE status='pending' ORDER BY created_at DESC"
            ).fetchall()
            return [self._row(r) for r in rows]
        finally:
            conn.close()

    def get(self, handoff_id: str) -> Optional[dict]:
        conn = self.connect()
        try:
            r = conn.execute(
                "SELECT * FROM handoffs WHERE handoff_id=?", (handoff_id,)
            ).fetchone()
            return self._row(r) if r else None
        finally:
            conn.close()

    def resolve(self, handoff_id: str) -> bool:
        conn = self.connect()
        try:
            cur = conn.execute(
                "UPDATE handoffs SET status='resolved', "
                "resolved_at=CURRENT_TIMESTAMP WHERE handoff_id=? AND status='pending'",
                (handoff_id,),
            )
            conn.commit()
            return cur.rowcount > 0
        finally:
            conn.close()

    def count_pending(self) -> int:
        conn = self.connect()
        try:
            return conn.execute(
                "SELECT COUNT(*) AS c FROM handoffs WHERE status='pending'"
            ).fetchone()["c"]
        finally:
            conn.close()
```
`app/hitl/__init__.py` 追加导出 `HandoffQueue`。

- [ ] **Step 4: 运行确认通过**

Run: `cd "D:/2026项目/ecom-service-agent" && .venv/Scripts/python.exe -m pytest tests/test_hitl_queue.py -v`
Expected: PASS（4 passed）。

- [ ] **Step 5: 提交**

```bash
git add app/hitl/queue.py app/hitl/__init__.py tests/test_hitl_queue.py
git commit -m "feat(hitl): HandoffQueue 坐席队列(SQLite)"
```

---

## Task 3: ManualMode + HitlManager

**Files:**
- Create: `app/hitl/manual_mode.py`
- Create: `app/hitl/manager.py`
- Modify: `app/hitl/__init__.py`
- Test: `tests/test_hitl_manual.py`

**Interfaces:**
- Produces:
  - `manual_mode.py`：`class ManualMode(timeout:int, now=time.time)`：`enter(sid)`、`exit(sid)`、`toggle(sid)->str("manual"|"auto")`、`is_manual(sid)->bool`（超时自动回落）
  - `manager.py`：`class HitlManager(queue, manual_mode, confidence_threshold, sensitive_intents=SENSITIVE_INTENTS)`：
    - `evaluate(intent, confidence, requires_human)->list`（委托 `should_escalate`）
    - `escalate(session_id, user_input, reply, intent, confidence, reasons, recent_context=None)->str`（建包 + 入队，返回 handoff_id）

- [ ] **Step 1: 写失败测试**

`tests/test_hitl_manual.py`：
```python
import itertools

from app.hitl.manual_mode import ManualMode
from app.hitl.manager import HitlManager
from app.hitl.queue import HandoffQueue


def test_toggle_and_is_manual():
    mm = ManualMode(timeout=3600, now=lambda: 100.0)
    assert mm.is_manual("s1") is False
    assert mm.toggle("s1") == "manual"
    assert mm.is_manual("s1") is True
    assert mm.toggle("s1") == "auto"
    assert mm.is_manual("s1") is False


def test_manual_mode_timeout():
    clock = {"t": 100.0}
    mm = ManualMode(timeout=60, now=lambda: clock["t"])
    mm.enter("s1")
    assert mm.is_manual("s1") is True
    clock["t"] = 100.0 + 61      # 超过 60s
    assert mm.is_manual("s1") is False   # 自动回落


def test_manager_evaluate_and_escalate(tmp_path):
    ids = itertools.count(1)
    q = HandoffQueue(str(tmp_path / "h.db"), id_factory=lambda: f"h{next(ids)}")
    q.init_schema()
    mgr = HitlManager(q, ManualMode(3600), confidence_threshold=0.6)
    reasons = mgr.evaluate("complaint", 0.9, False)
    assert reasons
    hid = mgr.escalate("s1", "投诉", "抱歉", "complaint", 0.9, reasons)
    assert q.get(hid)["session_id"] == "s1"
    assert q.count_pending() == 1
```

- [ ] **Step 2: 运行确认失败**

Run: `cd "D:/2026项目/ecom-service-agent" && .venv/Scripts/python.exe -m pytest tests/test_hitl_manual.py -v`
Expected: FAIL —— `ModuleNotFoundError: No module named 'app.hitl.manual_mode'`。

- [ ] **Step 3: 实现**

`app/hitl/manual_mode.py`：
```python
"""人工接管模式（内存态 + 超时自动回落）。借鉴 XianyuAutoAgent 的接管开关。"""

import time


class ManualMode:
    def __init__(self, timeout: int = 3600, now=time.time):
        self.timeout = timeout
        self._now = now
        self._entered: dict = {}   # session_id -> 进入时间

    def enter(self, session_id: str) -> None:
        self._entered[session_id] = self._now()

    def exit(self, session_id: str) -> None:
        self._entered.pop(session_id, None)

    def is_manual(self, session_id: str) -> bool:
        ts = self._entered.get(session_id)
        if ts is None:
            return False
        if self._now() - ts > self.timeout:
            self.exit(session_id)     # 超时自动回落自动模式
            return False
        return True

    def toggle(self, session_id: str) -> str:
        if self.is_manual(session_id):
            self.exit(session_id)
            return "auto"
        self.enter(session_id)
        return "manual"
```
`app/hitl/manager.py`：
```python
"""HITL 编排：升级判定 + 入队 + 人工模式。"""

from app.hitl.escalation import should_escalate, SENSITIVE_INTENTS
from app.hitl.handoff import build_handoff_bundle


class HitlManager:
    def __init__(self, queue, manual_mode, confidence_threshold: float,
                 sensitive_intents: set = SENSITIVE_INTENTS):
        self.queue = queue
        self.manual_mode = manual_mode
        self.confidence_threshold = confidence_threshold
        self.sensitive_intents = sensitive_intents

    def evaluate(self, intent: str, confidence: float, requires_human: bool) -> list:
        return should_escalate(intent, confidence, requires_human,
                               self.confidence_threshold, self.sensitive_intents)

    def escalate(self, session_id, user_input, reply, intent, confidence,
                 reasons, recent_context=None) -> str:
        bundle = build_handoff_bundle(session_id, user_input, reply, intent,
                                      confidence, reasons, recent_context)
        return self.queue.add(bundle)
```
`app/hitl/__init__.py` 追加导出 `ManualMode`、`HitlManager`。

- [ ] **Step 4: 运行确认通过**

Run: `cd "D:/2026项目/ecom-service-agent" && .venv/Scripts/python.exe -m pytest tests/test_hitl_manual.py -v`
Expected: PASS（3 passed）。

- [ ] **Step 5: 提交**

```bash
git add app/hitl/manual_mode.py app/hitl/manager.py app/hitl/__init__.py tests/test_hitl_manual.py
git commit -m "feat(hitl): ManualMode 人工接管(超时回落) + HitlManager 编排"
```

---

## Task 4: 接入服务层（升级 → handoff 事件 + 入队）

**Files:**
- Modify: `app/api/streaming.py`
- Modify: `app/observability/tracer.py`（`on_event` 支持 `handoff`）
- Test: `tests/test_streaming_hitl.py`

**Interfaces:**
- Consumes: `HitlManager`（Task 3）、`Tracer`（W2）。
- Produces: `run_agent_streaming(agent, user_input, tracer=None, session_id="", guard_pipeline=None, hitl=None)`：
  - 正常回复的 metadata 之后：若 `hitl` 非空，`reasons = hitl.evaluate(intent, confidence, requires_human)`；命中则读 `agent.raw_messages[-6:]` 作 recent_context，`hid = hitl.escalate(...)`，发 `{"type":"handoff","reasons":reasons,"handoff_id":hid}`。
  - `hitl=None` 或输入被护栏拦截：不评估升级。
  - Tracer `on_event` 对 `handoff` 事件记 `Span(kind="hitl", name="handoff")`。

- [ ] **Step 1: 写失败测试**

`tests/test_streaming_hitl.py`：
```python
import itertools

from app.api.streaming import run_agent_streaming
from app.hitl.manager import HitlManager
from app.hitl.manual_mode import ManualMode
from app.hitl.queue import HandoffQueue
from app.schemas.response import CustomerServiceResponse, IntentType


class FakeAgent:
    def __init__(self, intent=IntentType.COMPLAINT, confidence=0.4, requires_human=True):
        self.event_sink = None
        self.client = None
        self.raw_messages = [{"role": "user", "content": "你们太差了"}]
        self._r = CustomerServiceResponse(
            intent=intent, confidence=confidence, reply="非常抱歉给您带来不便",
            requires_human=requires_human, follow_up_question=None,
        )

    def chat(self, user_input):
        return self._r


def _hitl(tmp_path):
    ids = itertools.count(1)
    q = HandoffQueue(str(tmp_path / "h.db"), id_factory=lambda: f"h{next(ids)}")
    q.init_schema()
    return HitlManager(q, ManualMode(3600), confidence_threshold=0.6), q


def test_escalation_emits_handoff_and_enqueues(tmp_path):
    hitl, q = _hitl(tmp_path)
    events = list(run_agent_streaming(FakeAgent(), "投诉", hitl=hitl, session_id="s1"))
    handoff = [e for e in events if e["type"] == "handoff"]
    assert len(handoff) == 1
    assert handoff[0]["reasons"]
    assert q.count_pending() == 1
    assert q.get(handoff[0]["handoff_id"])["payload"]["recent_context"]


def test_no_escalation_for_normal(tmp_path):
    hitl, q = _hitl(tmp_path)
    agent = FakeAgent(intent=IntentType.ORDER_QUERY, confidence=0.95, requires_human=False)
    events = list(run_agent_streaming(agent, "查订单", hitl=hitl, session_id="s1"))
    assert not any(e["type"] == "handoff" for e in events)
    assert q.count_pending() == 0


def test_no_hitl_unchanged():
    agent = FakeAgent()
    events = list(run_agent_streaming(agent, "投诉"))
    assert [e["type"] for e in events] == ["reply", "metadata", "done"]
```

- [ ] **Step 2: 运行确认失败**

Run: `cd "D:/2026项目/ecom-service-agent" && .venv/Scripts/python.exe -m pytest tests/test_streaming_hitl.py -v`
Expected: FAIL —— `run_agent_streaming() got an unexpected keyword argument 'hitl'`。

- [ ] **Step 3: 实现**

`app/observability/tracer.py` 的 `on_event` 追加分支：
```python
        elif etype == "handoff":
            now = self._now()
            trace.spans.append(Span(
                span_id=self._id(), trace_id=trace.trace_id,
                name="handoff", kind="hitl",
                started_at=now, ended_at=now, latency_ms=0.0,
                meta={"reasons": event.get("reasons", [])},
            ))
```
`app/api/streaming.py`：给 `run_agent_streaming` 加 `hitl=None` 参数；在 `_normal_flow` 里 metadata 之后追加升级评估。把 `_normal_flow` 改为：
```python
    def _normal_flow(sink) -> str:
        result = agent.chat(user_input)
        reply = result.reply
        if guard_pipeline is not None:
            reply, out_results = guard_pipeline.check_output(reply)
            for gr in out_results:
                sink({"type": "guard", "stage": "output", "action": gr.action,
                      "guard": gr.guard, "reason": gr.reason})
        sink({"type": "reply", "content": reply})
        sink({"type": "metadata", "intent": result.intent.value,
              "confidence": result.confidence,
              "requires_human": result.requires_human,
              "follow_up_question": result.follow_up_question})
        if hitl is not None:
            reasons = hitl.evaluate(result.intent.value, result.confidence,
                                    result.requires_human)
            if reasons:
                recent = list(getattr(agent, "raw_messages", []))[-6:]
                hid = hitl.escalate(session_id, user_input, reply,
                                    result.intent.value, result.confidence,
                                    reasons, recent_context=recent)
                sink({"type": "handoff", "reasons": reasons, "handoff_id": hid})
        return result.intent.value
```
并把函数签名改为 `def run_agent_streaming(agent, user_input, tracer=None, session_id="", guard_pipeline=None, hitl=None)`。

- [ ] **Step 4: 运行确认通过**

Run: `cd "D:/2026项目/ecom-service-agent" && .venv/Scripts/python.exe -m pytest tests/test_streaming_hitl.py tests/test_streaming_guard.py tests/test_streaming_trace.py tests/test_streaming.py -q`
Expected: PASS（HITL 3 + guard 4 + trace 2 + W1 2 = 11 passed）。

- [ ] **Step 5: 提交**

```bash
git add app/api/streaming.py app/observability/tracer.py tests/test_streaming_hitl.py
git commit -m "feat(hitl): 服务层升级判定 → handoff 事件 + 入队 + Trace hitl span"
```

---

## Task 5: API 端点 + 人工接管短路 + 指标

**Files:**
- Modify: `app/api/app.py`
- Modify: `app/observability/metrics.py`
- Test: `tests/test_hitl_api.py`、`tests/test_metrics_guard.py`（追加 handoff 断言）

**Interfaces:**
- Consumes: `HandoffQueue`、`ManualMode`、`HitlManager`。
- Produces（`app.py`）：
  - 装配 `HitlManager`（queue + manual_mode），传给 `run_agent_streaming`。
  - `/api/chat`：命中 `manual_mode.is_manual(session_id)` 时短路，SSE 返回"人工处理中"reply + done，不调用 Agent。
  - `GET /api/handoffs` → 待处理列表；`POST /api/handoffs/{id}/resolve` → 解决；`POST /api/session/{sid}/takeover` → 切换人工/自动，返回 `{"mode": ...}`。
  - `metrics.py`：新增 `handoffs`（`kind=="hitl"` span 数）、`escalation_rate`。

- [ ] **Step 1: 写失败测试**

`tests/test_hitl_api.py`：
```python
from fastapi.testclient import TestClient

from app.api.app import create_app
from app.api.session_manager import SessionManager
from app.observability.store import TraceStore
from app.hitl.queue import HandoffQueue
from app.hitl.manual_mode import ManualMode
from app.hitl.manager import HitlManager
from app.schemas.response import CustomerServiceResponse, IntentType


class FakeAgent:
    def __init__(self, session_path=None):
        self.event_sink = None
        self.client = None
        self.raw_messages = []
        self.chat_called = False

    def chat(self, user_input):
        self.chat_called = True
        return CustomerServiceResponse(
            intent=IntentType.GREETING, confidence=0.9, reply="您好",
            requires_human=False, follow_up_question=None,
        )


def _client(tmp_path):
    store = TraceStore(str(tmp_path / "tr.db")); store.init_schema()
    q = HandoffQueue(str(tmp_path / "h.db")); q.init_schema()
    q.add({"session_id": "s1", "intent": "complaint", "reasons": ["敏感意图"]})
    mm = ManualMode(3600)
    hitl = HitlManager(q, mm, 0.6)
    mgr = SessionManager(agent_factory=lambda p: FakeAgent(p))
    app = create_app(session_manager=mgr, trace_store=store, hitl=hitl)
    return TestClient(app), q, mm


def test_list_and_resolve_handoffs(tmp_path):
    c, q, _ = _client(tmp_path)
    lst = c.get("/api/handoffs").json()
    assert len(lst) == 1
    hid = lst[0]["handoff_id"]
    assert c.post(f"/api/handoffs/{hid}/resolve").json()["status"] == "resolved"
    assert c.get("/api/handoffs").json() == []


def test_takeover_toggle(tmp_path):
    c, q, mm = _client(tmp_path)
    r = c.post("/api/session/s1/takeover").json()
    assert r["mode"] == "manual"
    assert mm.is_manual("s1") is True
    r2 = c.post("/api/session/s1/takeover").json()
    assert r2["mode"] == "auto"


def test_manual_mode_short_circuits_chat(tmp_path):
    c, q, mm = _client(tmp_path)
    mm.enter("s1")
    resp = c.post("/api/chat", json={"session_id": "s1", "message": "在吗"})
    body = resp.text
    assert "人工" in body           # 返回人工处理中提示
    # Agent 不应被调用：拿该会话 agent 实例检查
```

- [ ] **Step 2: 运行确认失败**

Run: `cd "D:/2026项目/ecom-service-agent" && .venv/Scripts/python.exe -m pytest tests/test_hitl_api.py -v`
Expected: FAIL —— `create_app() got an unexpected keyword argument 'hitl'`。

- [ ] **Step 3: 实现**

`app/observability/metrics.py`：在 guard 统计附近加：
```python
    hitl_spans = [s for s in spans if s["kind"] == "hitl"]
```
返回 dict 追加：
```python
        "handoffs": len(hitl_spans),
        "escalation_rate": (len(hitl_spans) / total) if total else 0.0,
```
`app/api/app.py`：顶部 import 追加：
```python
from app.hitl.manager import HitlManager
from app.hitl.manual_mode import ManualMode
from app.hitl.queue import HandoffQueue
```
`create_app` 签名加 `hitl: Optional[HitlManager] = None`；装配：
```python
    if hitl is None and settings.hitl_enabled:
        _hq = HandoffQueue(); _hq.init_schema()
        hitl = HitlManager(_hq, ManualMode(settings.manual_mode_timeout),
                           settings.hitl_confidence_threshold)
```
`/api/chat` 端点开头加人工接管短路：
```python
    @app.post("/api/chat")
    def chat(req: ChatRequest):
        if hitl is not None and hitl.manual_mode.is_manual(req.session_id):
            def manual_stream():
                yield _sse_frame({"type": "reply",
                                  "content": "🎧 当前会话已转由人工客服处理，请稍候…"})
                yield _sse_frame({"type": "done"})
            return StreamingResponse(manual_stream(), media_type="text/event-stream")

        agent = manager.get_or_create(req.session_id)
        lock = manager.get_lock(req.session_id)

        def event_stream():
            with lock:
                for event in run_agent_streaming(
                    agent, req.message, tracer=tracer,
                    session_id=req.session_id, guard_pipeline=guard_pipeline, hitl=hitl,
                ):
                    yield _sse_frame(event)

        return StreamingResponse(event_stream(), media_type="text/event-stream")
```
新增端点：
```python
    @app.get("/api/handoffs")
    def handoffs():
        return hitl.queue.list_pending() if hitl else []

    @app.post("/api/handoffs/{handoff_id}/resolve")
    def resolve_handoff(handoff_id: str):
        ok = hitl.queue.resolve(handoff_id) if hitl else False
        return {"status": "resolved" if ok else "not_found"}

    @app.post("/api/session/{session_id}/takeover")
    def takeover(session_id: str):
        if hitl is None:
            return {"mode": "auto"}
        return {"mode": hitl.manual_mode.toggle(session_id)}
```

- [ ] **Step 4: 运行确认通过 + W3-HITL 全量**

Run: `cd "D:/2026项目/ecom-service-agent" && .venv/Scripts/python.exe -m pytest tests/test_hitl_escalation.py tests/test_hitl_queue.py tests/test_hitl_manual.py tests/test_streaming_hitl.py tests/test_hitl_api.py tests/test_dashboard_api.py tests/test_api.py -q`
Expected: 全绿（5+4+3+3+3+3+4 = 25 passed）。

- [ ] **Step 5: 提交**

```bash
git add app/api/app.py app/observability/metrics.py tests/test_hitl_api.py
git commit -m "feat(hitl): 坐席队列/接管端点 + 人工模式短路 + 转人工指标"
```

---

## Task 6: 坐席面板 + 端到端

**Files:**
- Modify: `web/chat.html`（新增「🎧 坐席」标签 + 接管开关 + handoff 事件 + 看板转人工卡片）
- Modify: `README.md`
- Test: 前端回归 + 人工端到端

**Interfaces:**
- Consumes: `/api/handoffs`、`/api/handoffs/{id}/resolve`、`/api/session/{sid}/takeover`、`/api/metrics`。

- [ ] **Step 1: 前端改造**

`web/chat.html`：
1. 顶部 tabs 加第三个：`<div class="tab" id="tab-seat" onclick="switchTab('seat')">🎧 坐席</div>`。
2. `switchTab` 支持 `seat`：显示 `#seat-view`、隐藏其余；进入时 `loadSeat()`。
3. 新增 `<section id="seat-view" hidden>`：一个"人工接管当前会话"开关按钮 + 待处理列表容器。
4. 聊天流事件处理加 handoff：
```javascript
      else if (ev.type === "handoff") traceLine(trace, "🎧 已转人工，原因：" + (ev.reasons||[]).join("、"));
```
5. 看板 cards 追加：`card("转人工数", m.handoffs), card("转人工率", (m.escalation_rate*100).toFixed(1)+"%")`。
6. 坐席逻辑：
```javascript
async function loadSeat() {
  const list = await (await fetch("/api/handoffs")).json();
  const box = document.getElementById("seat-list");
  box.innerHTML = list.length ? list.map(h => `
    <div class="card" style="margin-bottom:8px">
      <div><b>会话 ${h.session_id}</b> · 意图 ${h.intent||'-'} · ${h.created_at||''}</div>
      <div style="color:#c0392b;font-size:13px">原因：${(h.reasons||[]).join("、")}</div>
      <div style="font-size:13px;color:#555">用户：${(h.payload&&h.payload.user_input)||''}</div>
      <div style="font-size:13px;color:#555">Agent：${(h.payload&&h.payload.reply)||''}</div>
      <button style="margin-top:6px;padding:4px 10px" onclick="resolveHandoff('${h.handoff_id}')">标记已解决</button>
    </div>`).join("") : "<p style='color:#888'>暂无待接管会话</p>";
}
async function resolveHandoff(id) {
  await fetch("/api/handoffs/" + id + "/resolve", {method:"POST"});
  loadSeat();
}
async function toggleTakeover() {
  const r = await (await fetch("/api/session/" + SESSION_ID + "/takeover", {method:"POST"})).json();
  document.getElementById("takeover-state").textContent =
    r.mode === "manual" ? "当前：人工接管中（Agent 已暂停）" : "当前：自动（AI 回复）";
}
```
`#seat-view` 结构：
```html
  <section id="seat-view" hidden>
    <div class="card" style="margin-bottom:12px">
      <button onclick="toggleTakeover()">切换本会话 人工/自动</button>
      <span id="takeover-state" style="margin-left:10px;color:#555">当前：自动（AI 回复）</span>
    </div>
    <h3>待接管会话 <button onclick="loadSeat()" style="padding:4px 10px">刷新</button></h3>
    <div id="seat-list"></div>
  </section>
```

- [ ] **Step 2: 前端回归**

Run: `cd "D:/2026项目/ecom-service-agent" && .venv/Scripts/python.exe -m pytest tests/test_dashboard_api.py -q`
Expected: PASS。

- [ ] **Step 3: W3-HITL 全量测试**

Run: `cd "D:/2026项目/ecom-service-agent" && .venv/Scripts/python.exe -m pytest tests/test_hitl_escalation.py tests/test_hitl_queue.py tests/test_hitl_manual.py tests/test_streaming_hitl.py tests/test_hitl_api.py -q`
Expected: 全绿（18 passed）。

- [ ] **Step 4: 端到端（需 API Key）**

```bash
.venv/Scripts/python.exe run_api.py
```
- 聊天里发投诉类消息（如"你们这什么破客服，我要投诉"）→ 回复后思考区出现 `🎧 已转人工`；
- 切「🎧 坐席」标签 → 待接管列表出现该会话，点"标记已解决"消失；
- 点"切换本会话 人工/自动"→ 变人工后再在聊天发消息 → 收到"人工处理中"提示、Agent 不回复；再切回自动恢复。

- [ ] **Step 5: 更新 README + 提交**

README 补 HITL 说明。提交：
```bash
git add web/chat.html README.md
git commit -m "feat(hitl): 坐席面板(队列+接管开关) + 转人工看板卡片 + README"
```

---

## Self-Review（作者自查）

- **Spec 覆盖**：对应设计文档第 2 节③Human-in-the-Loop（升级触发器：低置信/护栏/敏感意图；交接上下文包；坐席面板）与第 5 节 W3「转人工判定与上下文交接 + 坐席面板」。护栏触发升级：当前以"输出脱敏 + 低置信"覆盖大部分；输入拦截已在护栏侧安全兜底，不再重复升级。连续工具失败触发留作后续增强（已在文档注明）。✅
- **不动核心**：HITL 仅在 `app/hitl/` 与服务层；`chat.py` 零改动。✅
- **不破坏既有**：`run_agent_streaming` 新增 `hitl` 默认 None；`create_app` 新增 `hitl` 默认 None；纳入 W1/W2/W3-护栏回归。✅
- **Xianyu 借鉴落地**：`ManualMode` 的接管开关 + 超时回落，对应 Xianyu 的关键词接管 + `manual_mode_timeout`。✅
- **类型一致性**：`HitlManager.evaluate/escalate`、`HandoffQueue.add/list_pending/resolve/get/count_pending`、`ManualMode.is_manual/toggle` 在服务层与端点调用签名一致；handoff 事件字段（reasons/handoff_id）在发射端、tracer、前端一致。✅

---

## 完成即达成的里程碑

发一句投诉，Agent 回复后自动转人工、在坐席面板出现带上下文的待接管会话；坐席可一键接管该会话（Agent 暂停、真人处理），超时自动回落。至此 W3（护栏 + 人机协作）完整，项目具备"安全 + 兜底"的上线底线。下一步 W3.5 生产加固（鉴权/限流/成本上限 + 规则/LLM 混合路由）。
```
