# W3.5：生产加固 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 给服务加公网上线必备的加固层：**限流**（防刷）、**成本上限**（防烧爆 API Key）、**规则快路径路由**（高频简单意图规则秒回、零 LLM 成本，借鉴 Xianyu 的规则优先 + LLM 兜底）、**管理端鉴权**（护栏/看板/坐席等管理接口加令牌）。

**Architecture:** **全部在服务层，`chat.py` 零改动**。新增 `app/hardening/` 模块（纯内存/规则，离线可测，均可注入时钟）：`RateLimiter`（按会话滑窗限流）、`CostGuard`（按天请求预算）、`FastPathRouter`（规则命中直接返回，跳过 Agent）、`admin_auth`（管理接口令牌依赖）。`/api/chat` 端点按序挂载：限流 → 人工接管 → 快路径 → 成本上限 → 正常 Agent。管理端点加鉴权依赖。均可通过 settings 开关/阈值配置，默认对本地友好。

**Tech Stack:** Python 3.11+、内存数据结构 + 正则、FastAPI（依赖注入 + Header）、pytest（离线，注入时钟）。

## Global Constraints

- **Python 3.11+**；解释器 `D:/2026项目/ecom-service-agent/.venv/Scripts/python.exe`。
- **`chat.py`/`orchestrator.py` 核心零改动**：加固只出现在 `app/hardening/` 与 `app/api/`。
- **本地友好默认值**：`admin_token` 默认空（=不鉴权，本地/演示可用）；限流/成本阈值给宽松默认，不影响正常体验。
- **不破坏 W1–W3**：`/api/chat` 新增前置检查都短路返回标准 SSE（reply+done），既有测试须继续通过。
- **测试离线**：限流/成本注入假时钟；快路径/鉴权纯逻辑。不依赖 LLM/网络。
- **短路友好**：限流/成本/人工/快路径命中，都以标准 SSE（`reply` + `done`）返回，前端无需特殊处理。
- **Windows 友好**：不依赖 `datetime` 也能测（按 `now // 86400` 分天）。

---

## File Structure

- `app/config/settings.py` — 修改：新增 `admin_token` / `rate_limit_per_min` / `daily_request_budget` / `fast_path_enabled`。
- `app/hardening/__init__.py` — 新建：导出四件套。
- `app/hardening/rate_limit.py` — 新建：`RateLimiter`（会话滑窗）。
- `app/hardening/cost_guard.py` — 新建：`CostGuard`（按天请求预算）。
- `app/hardening/fast_path.py` — 新建：`match_fast_path`（规则秒回）。
- `app/hardening/auth.py` — 新建：`make_admin_auth`（Header 令牌依赖）。
- `app/api/app.py` — 修改：`/api/chat` 挂载限流/成本/快路径；管理端点加鉴权。
- `web/chat.html` — 修改：管理类请求带上 `X-Admin-Token`（从 localStorage 取，默认空）。
- `README.md` — 修改：加固说明。
- `tests/test_rate_limit.py` / `test_cost_guard.py` / `test_fast_path.py` / `test_hardening_api.py` — 新建。

---

## Task 1: RateLimiter（会话滑窗限流）

**Files:**
- Modify: `app/config/settings.py`
- Create: `app/hardening/rate_limit.py`
- Create: `app/hardening/__init__.py`（先导出 RateLimiter）
- Test: `tests/test_rate_limit.py`

**Interfaces:**
- Produces:
  - `settings.rate_limit_per_min: int=20`、`daily_request_budget: int=500`、`admin_token: str=""`、`fast_path_enabled: bool=True`
  - `rate_limit.py`：`class RateLimiter(max_per_window:int, window_seconds:int=60, now=time.time)`：`allow(key:str)->bool`（在窗口内计数，未超返回 True 并记录，超了返回 False）

- [ ] **Step 1: 写失败测试**

`tests/test_rate_limit.py`：
```python
from app.hardening.rate_limit import RateLimiter


def test_allows_within_limit():
    rl = RateLimiter(max_per_window=3, window_seconds=60, now=lambda: 1000.0)
    assert rl.allow("s1") is True
    assert rl.allow("s1") is True
    assert rl.allow("s1") is True
    assert rl.allow("s1") is False   # 第4次超限


def test_window_slides():
    clock = {"t": 1000.0}
    rl = RateLimiter(max_per_window=2, window_seconds=60, now=lambda: clock["t"])
    assert rl.allow("s1") is True
    assert rl.allow("s1") is True
    assert rl.allow("s1") is False
    clock["t"] = 1000.0 + 61          # 窗口滑过
    assert rl.allow("s1") is True


def test_keys_isolated():
    rl = RateLimiter(max_per_window=1, window_seconds=60, now=lambda: 1000.0)
    assert rl.allow("s1") is True
    assert rl.allow("s2") is True     # 不同会话独立
    assert rl.allow("s1") is False
```

- [ ] **Step 2: 运行确认失败**

Run: `cd "D:/2026项目/ecom-service-agent" && .venv/Scripts/python.exe -m pytest tests/test_rate_limit.py -v`
Expected: FAIL —— `ModuleNotFoundError: No module named 'app.hardening'`。

- [ ] **Step 3: 实现**

`app/config/settings.py` 在 HITL 配置块下方加：
```python
    # 生产加固（W3.5）
    admin_token: str = ""              # 管理接口令牌；空=本地不鉴权
    rate_limit_per_min: int = 20       # 每会话每分钟最大请求数
    daily_request_budget: int = 500    # 每日全局请求上限（成本兜底）
    fast_path_enabled: bool = True     # 规则快路径（高频简单意图秒回）
```
`app/hardening/rate_limit.py`：
```python
"""按 key（会话）滑动窗口限流。"""

import time
from collections import defaultdict, deque


class RateLimiter:
    def __init__(self, max_per_window: int, window_seconds: int = 60, now=time.time):
        self.max = max_per_window
        self.window = window_seconds
        self._now = now
        self._hits = defaultdict(deque)   # key -> 时间戳队列

    def allow(self, key: str) -> bool:
        now = self._now()
        dq = self._hits[key]
        while dq and now - dq[0] >= self.window:
            dq.popleft()
        if len(dq) >= self.max:
            return False
        dq.append(now)
        return True
```
`app/hardening/__init__.py`：
```python
from app.hardening.rate_limit import RateLimiter

__all__ = ["RateLimiter"]
```

- [ ] **Step 4: 运行确认通过**

Run: `cd "D:/2026项目/ecom-service-agent" && .venv/Scripts/python.exe -m pytest tests/test_rate_limit.py -v`
Expected: PASS（3 passed）。

- [ ] **Step 5: 提交**

```bash
git add app/config/settings.py app/hardening/rate_limit.py app/hardening/__init__.py tests/test_rate_limit.py
git commit -m "feat(harden): 会话滑窗限流 RateLimiter"
```

---

## Task 2: CostGuard（按天请求预算）

**Files:**
- Create: `app/hardening/cost_guard.py`
- Modify: `app/hardening/__init__.py`
- Test: `tests/test_cost_guard.py`

**Interfaces:**
- Produces: `class CostGuard(max_requests_per_day:int, now=time.time)`：
  - `allow()->bool`（当天未超预算则计数并返回 True，超了返回 False）
  - `spent()->int`（当天已用请求数）
  - 跨天自动重置（按 `int(now//86400)` 分天）

- [ ] **Step 1: 写失败测试**

`tests/test_cost_guard.py`：
```python
from app.hardening.cost_guard import CostGuard


def test_allows_until_budget():
    cg = CostGuard(max_requests_per_day=2, now=lambda: 100.0)
    assert cg.allow() is True
    assert cg.allow() is True
    assert cg.allow() is False
    assert cg.spent() == 2


def test_resets_next_day():
    clock = {"t": 100.0}
    cg = CostGuard(max_requests_per_day=1, now=lambda: clock["t"])
    assert cg.allow() is True
    assert cg.allow() is False
    clock["t"] = 100.0 + 86400        # 第二天
    assert cg.allow() is True
    assert cg.spent() == 1
```

- [ ] **Step 2: 运行确认失败**

Run: `cd "D:/2026项目/ecom-service-agent" && .venv/Scripts/python.exe -m pytest tests/test_cost_guard.py -v`
Expected: FAIL —— `ModuleNotFoundError`。

- [ ] **Step 3: 实现**

`app/hardening/cost_guard.py`：
```python
"""按天请求预算，成本兜底（防刷爆 API Key）。"""

import time


class CostGuard:
    def __init__(self, max_requests_per_day: int, now=time.time):
        self.max = max_requests_per_day
        self._now = now
        self._day = None
        self._count = 0

    def _today(self) -> int:
        return int(self._now() // 86400)

    def _roll(self) -> None:
        d = self._today()
        if d != self._day:
            self._day = d
            self._count = 0

    def allow(self) -> bool:
        self._roll()
        if self._count >= self.max:
            return False
        self._count += 1
        return True

    def spent(self) -> int:
        self._roll()
        return self._count
```
`app/hardening/__init__.py` 追加导出 `CostGuard`。

- [ ] **Step 4: 运行确认通过**

Run: `cd "D:/2026项目/ecom-service-agent" && .venv/Scripts/python.exe -m pytest tests/test_cost_guard.py -v`
Expected: PASS（2 passed）。

- [ ] **Step 5: 提交**

```bash
git add app/hardening/cost_guard.py app/hardening/__init__.py tests/test_cost_guard.py
git commit -m "feat(harden): 按天请求预算 CostGuard"
```

---

## Task 3: FastPathRouter（规则秒回，规则优先 + LLM 兜底）

**Files:**
- Create: `app/hardening/fast_path.py`
- Modify: `app/hardening/__init__.py`
- Test: `tests/test_fast_path.py`

**Interfaces:**
- Produces: `match_fast_path(text:str)->Optional[dict]`：命中高频简单意图（打招呼/感谢/告别）返回 `{"intent":..., "reply":...}`，否则 None。

- [ ] **Step 1: 写失败测试**

`tests/test_fast_path.py`：
```python
from app.hardening.fast_path import match_fast_path


def test_greeting_hits():
    r = match_fast_path("你好")
    assert r and r["intent"] == "greeting"
    assert r["reply"]


def test_thanks_hits():
    assert match_fast_path("谢谢啦")["intent"] == "thanks"


def test_bye_hits():
    assert match_fast_path("再见")["intent"] == "bye"


def test_business_query_misses():
    assert match_fast_path("我的订单 ORD-20240115-001 发货了吗") is None
    assert match_fast_path("这件衣服质量有问题要退货") is None
```

- [ ] **Step 2: 运行确认失败**

Run: `cd "D:/2026项目/ecom-service-agent" && .venv/Scripts/python.exe -m pytest tests/test_fast_path.py -v`
Expected: FAIL —— `ModuleNotFoundError`。

- [ ] **Step 3: 实现**

`app/hardening/fast_path.py`：
```python
"""规则快路径：高频简单意图直接规则回复，跳过 Agent（省 LLM 成本/延迟）。

规则优先 + LLM 兜底：命中则秒回；未命中返回 None，交给正常 Agent 流程。
借鉴 XianyuAutoAgent 的规则路由思路。
"""

import re
from typing import Optional

# 每项：(意图, 判定正则, 回复)。仅匹配"纯寒暄/感谢/告别"类短消息。
_RULES = [
    ("greeting",
     re.compile(r"^(你好|您好|哈喽|嗨|在吗|在么|hi|hello)[\s!！~。.]*$", re.IGNORECASE),
     "您好，我是并夕夕智能客服小夕 😊 请问需要查订单、看物流、咨询商品还是售后呢？"),
    ("thanks",
     re.compile(r"(谢谢|感谢|多谢|thx|thanks|辛苦了)", re.IGNORECASE),
     "不客气～能帮到您就好 😊 还有其他可以帮您的吗？"),
    ("bye",
     re.compile(r"^(再见|拜拜|bye|goodbye|没事了|不用了)[\s!！~。.]*$", re.IGNORECASE),
     "感谢您的咨询，祝您购物愉快，欢迎下次光临并夕夕 👋"),
]


def match_fast_path(text: str) -> Optional[dict]:
    t = (text or "").strip()
    if not t or len(t) > 20:      # 过长的一律走 Agent
        return None
    for intent, pat, reply in _RULES:
        if pat.search(t):
            return {"intent": intent, "reply": reply}
    return None
```
`app/hardening/__init__.py` 追加导出 `match_fast_path`。

- [ ] **Step 4: 运行确认通过**

Run: `cd "D:/2026项目/ecom-service-agent" && .venv/Scripts/python.exe -m pytest tests/test_fast_path.py -v`
Expected: PASS（4 passed）。

- [ ] **Step 5: 提交**

```bash
git add app/hardening/fast_path.py app/hardening/__init__.py tests/test_fast_path.py
git commit -m "feat(harden): 规则快路径 FastPathRouter(高频意图秒回)"
```

---

## Task 4: 管理端鉴权 admin_auth

**Files:**
- Create: `app/hardening/auth.py`
- Modify: `app/hardening/__init__.py`
- Test: `tests/test_hardening_api.py`（本任务先建，含鉴权用例）

**Interfaces:**
- Produces: `make_admin_auth(token:str)->Callable`：返回一个 FastAPI 依赖；`token` 为空时放行所有请求，非空时要求请求头 `X-Admin-Token` 匹配，否则抛 `HTTPException(401)`。

- [ ] **Step 1: 写失败测试**

`tests/test_hardening_api.py`：
```python
from fastapi import Depends, FastAPI
from fastapi.testclient import TestClient

from app.hardening.auth import make_admin_auth


def _app(token):
    app = FastAPI()
    auth = make_admin_auth(token)

    @app.get("/admin", dependencies=[Depends(auth)])
    def admin():
        return {"ok": True}

    return TestClient(app)


def test_no_token_allows_all():
    c = _app("")
    assert c.get("/admin").status_code == 200


def test_token_required_when_set():
    c = _app("secret")
    assert c.get("/admin").status_code == 401
    assert c.get("/admin", headers={"X-Admin-Token": "wrong"}).status_code == 401
    assert c.get("/admin", headers={"X-Admin-Token": "secret"}).status_code == 200
```

- [ ] **Step 2: 运行确认失败**

Run: `cd "D:/2026项目/ecom-service-agent" && .venv/Scripts/python.exe -m pytest tests/test_hardening_api.py -v`
Expected: FAIL —— `ModuleNotFoundError: No module named 'app.hardening.auth'`。

- [ ] **Step 3: 实现**

`app/hardening/auth.py`：
```python
"""管理接口鉴权：X-Admin-Token 令牌校验（令牌为空时不鉴权）。"""

from fastapi import Header, HTTPException


def make_admin_auth(token: str):
    def _dep(x_admin_token: str = Header(default="")):
        if token and x_admin_token != token:
            raise HTTPException(status_code=401, detail="需要有效的管理令牌")
    return _dep
```
`app/hardening/__init__.py` 追加导出 `make_admin_auth`。

- [ ] **Step 4: 运行确认通过**

Run: `cd "D:/2026项目/ecom-service-agent" && .venv/Scripts/python.exe -m pytest tests/test_hardening_api.py -v`
Expected: PASS（2 passed）。

- [ ] **Step 5: 提交**

```bash
git add app/hardening/auth.py app/hardening/__init__.py tests/test_hardening_api.py
git commit -m "feat(harden): 管理接口令牌鉴权 admin_auth"
```

---

## Task 5: 接入 app.py（限流/成本/快路径 + 管理端鉴权）

**Files:**
- Modify: `app/api/app.py`
- Test: `tests/test_hardening_api.py`（追加集成用例）

**Interfaces:**
- Consumes: `RateLimiter`、`CostGuard`、`match_fast_path`、`make_admin_auth`（Task 1-4）。
- Produces（`app.py`）：
  - 装配 `RateLimiter(settings.rate_limit_per_min)`、`CostGuard(settings.daily_request_budget)`、`admin_auth=make_admin_auth(settings.admin_token)`。
  - `/api/chat` 顺序：限流超限 → SSE「请求过于频繁」；人工接管（既有）；快路径命中 → SSE 秒回；成本超限 → SSE「今日已达上限」；否则正常。
  - 管理端点 `/api/metrics`、`/api/traces`、`/api/traces/{id}`、`/api/handoffs`、`/api/handoffs/{id}/resolve`、`/api/session/{id}/takeover`、`/api/session/reset` 加 `dependencies=[Depends(admin_auth)]`。

- [ ] **Step 1: 追加失败测试**

在 `tests/test_hardening_api.py` 追加（复用真实 create_app + fake agent）：
```python
from app.api.app import create_app
from app.api.session_manager import SessionManager
from app.observability.store import TraceStore
from app.schemas.response import CustomerServiceResponse, IntentType


class _FakeAgent:
    def __init__(self, session_path=None):
        self.event_sink = None; self.client = None; self.raw_messages = []
        self.chat_called = False

    def chat(self, user_input):
        self.chat_called = True
        return CustomerServiceResponse(intent=IntentType.ORDER_QUERY, confidence=0.9,
            reply="已发货", requires_human=False, follow_up_question=None)


def _client(tmp_path, **over):
    store = TraceStore(str(tmp_path / "tr.db")); store.init_schema()
    mgr = SessionManager(agent_factory=lambda p: _FakeAgent(p))
    app = create_app(session_manager=mgr, trace_store=store, hitl=None, **over)
    return TestClient(app)


def test_fast_path_skips_agent(tmp_path):
    c = _client(tmp_path)
    r = c.post("/api/chat", json={"session_id": "s1", "message": "你好"})
    assert "小夕" in r.text            # 规则秒回
    # 未走 Agent（无 metadata 事件，只有 reply+done）
    assert "metadata" not in r.text


def test_admin_endpoint_guarded(tmp_path):
    c = _client(tmp_path, admin_token="secret")
    assert c.get("/api/metrics").status_code == 401
    assert c.get("/api/metrics", headers={"X-Admin-Token": "secret"}).status_code == 200


def test_chat_open_without_admin_token(tmp_path):
    c = _client(tmp_path, admin_token="secret")
    # 顾客对话端点不需要管理令牌
    r = c.post("/api/chat", json={"session_id": "s1", "message": "查订单"})
    assert r.status_code == 200
```

- [ ] **Step 2: 运行确认失败**

Run: `cd "D:/2026项目/ecom-service-agent" && .venv/Scripts/python.exe -m pytest tests/test_hardening_api.py -v`
Expected: FAIL —— `create_app() got an unexpected keyword argument 'admin_token'`。

- [ ] **Step 3: 实现**

`app/api/app.py`：
- 顶部 import 追加：
```python
from fastapi import Depends
from app.hardening.rate_limit import RateLimiter
from app.hardening.cost_guard import CostGuard
from app.hardening.fast_path import match_fast_path
from app.hardening.auth import make_admin_auth
```
- `create_app` 签名加参数：`admin_token: Optional[str] = None`。
- 在 guard/hitl 装配后加：
```python
    rate_limiter = RateLimiter(settings.rate_limit_per_min)
    cost_guard = CostGuard(settings.daily_request_budget)
    _admin_token = admin_token if admin_token is not None else settings.admin_token
    admin_auth = make_admin_auth(_admin_token)
```
- 抽一个小工具产出单条回复的 SSE：
```python
    def _reply_stream(text: str):
        def gen():
            yield _sse_frame({"type": "reply", "content": text})
            yield _sse_frame({"type": "done"})
        return StreamingResponse(gen(), media_type="text/event-stream")
```
- 改写 `/api/chat` 开头（在 manual 判断之前加限流；manual 之后加快路径与成本）：
```python
    @app.post("/api/chat")
    def chat(req: ChatRequest):
        if not rate_limiter.allow(req.session_id):
            return _reply_stream("⏳ 您发送得太快啦，请稍后再试～")

        if hitl is not None and hitl.manual_mode.is_manual(req.session_id):
            return _reply_stream("🎧 当前会话已转由人工客服处理，请稍候…")

        if settings.fast_path_enabled:
            fp = match_fast_path(req.message)
            if fp:
                return _reply_stream(fp["reply"])

        if not cost_guard.allow():
            return _reply_stream("🛑 今日服务已达使用上限，请明天再来～")

        agent = manager.get_or_create(req.session_id)
        lock = manager.get_lock(req.session_id)

        def event_stream():
            with lock:
                for event in run_agent_streaming(
                    agent, req.message, tracer=tracer,
                    session_id=req.session_id, guard_pipeline=guard_pipeline, hitl=hitl,
                ):
                    yield _sse_frame(event)

        return StreamingResponse(event_stream(), media_type="text/event-stream")
```
- 给管理端点加鉴权依赖（在各自装饰器加 `dependencies=[Depends(admin_auth)]`），例如：
```python
    @app.get("/api/metrics", dependencies=[Depends(admin_auth)])
    def metrics(): ...
    @app.get("/api/traces", dependencies=[Depends(admin_auth)])
    def traces(...): ...
    @app.get("/api/traces/{trace_id}", dependencies=[Depends(admin_auth)])
    def trace_detail(...): ...
    @app.get("/api/handoffs", dependencies=[Depends(admin_auth)])
    def handoffs(): ...
    @app.post("/api/handoffs/{handoff_id}/resolve", dependencies=[Depends(admin_auth)])
    def resolve_handoff(...): ...
    @app.post("/api/session/{session_id}/takeover", dependencies=[Depends(admin_auth)])
    def takeover(...): ...
    @app.post("/api/session/reset", dependencies=[Depends(admin_auth)])
    def reset(...): ...
```
> `/api/chat`、`/`、`/dashboard`、`/api/health` 不加鉴权（面向顾客/公开）。

- [ ] **Step 4: 运行确认通过 + 回归**

Run: `cd "D:/2026项目/ecom-service-agent" && .venv/Scripts/python.exe -m pytest tests/test_hardening_api.py tests/test_hitl_api.py tests/test_dashboard_api.py tests/test_api.py -q`
Expected: 全绿。
> 注：`test_hitl_api.py`/`test_dashboard_api.py` 用默认 `admin_token=""`（不鉴权），既有断言不受影响。

- [ ] **Step 5: 提交**

```bash
git add app/api/app.py tests/test_hardening_api.py
git commit -m "feat(harden): /api/chat 挂载限流/快路径/成本上限 + 管理端点鉴权"
```

---

## Task 6: 前端管理令牌 + 端到端 + README

**Files:**
- Modify: `web/chat.html`（管理类 fetch 带 `X-Admin-Token`）
- Modify: `README.md`
- Test: 前端回归 + 人工端到端

**Interfaces:**
- Consumes: 管理端点（可能需令牌）。

- [ ] **Step 1: 前端改造**

`web/chat.html`：新增取令牌 + 统一带头的封装，并把看板/坐席里的管理类 `fetch` 换成它：
```javascript
const ADMIN_TOKEN = localStorage.getItem("admin_token") || "";
function adminFetch(url, opts) {
  opts = opts || {};
  opts.headers = Object.assign({}, opts.headers, ADMIN_TOKEN ? {"X-Admin-Token": ADMIN_TOKEN} : {});
  return fetch(url, opts);
}
```
把 `loadDash`/`detail`/`loadSeat`/`resolveHandoff`/`toggleTakeover`/重置里对 `/api/metrics`、`/api/traces...`、`/api/handoffs...`、`/api/session/*/takeover`、`/api/session/reset` 的 `fetch(` 改为 `adminFetch(`（`/api/chat` 保持普通 `fetch`）。
> 本地默认 `admin_token=""`，`ADMIN_TOKEN` 为空、不加头，一切照常；启用鉴权时在浏览器控制台 `localStorage.setItem("admin_token","你的令牌")` 即可。

- [ ] **Step 2: 前端回归**

Run: `cd "D:/2026项目/ecom-service-agent" && .venv/Scripts/python.exe -m pytest tests/test_dashboard_api.py tests/test_hitl_api.py -q`
Expected: PASS。

- [ ] **Step 3: W3.5 全量测试**

Run: `cd "D:/2026项目/ecom-service-agent" && .venv/Scripts/python.exe -m pytest tests/test_rate_limit.py tests/test_cost_guard.py tests/test_fast_path.py tests/test_hardening_api.py -q`
Expected: 全绿（3+2+4+5 = 14 passed）。

- [ ] **Step 4: 端到端（需 API Key）**

```bash
.venv/Scripts/python.exe run_api.py
```
- 发「你好」→ **秒回**(规则快路径,无 LLM 延迟、看板 token 不增)；
- 连续快速发很多条 → 触发「发送太快」提示；
- （可选）`.env` 设 `ADMIN_TOKEN=xxx` 重启 → 直接开 `/dashboard` 拉数据会 401，控制台 `localStorage.setItem("admin_token","xxx")` 后刷新恢复。

- [ ] **Step 5: 更新 README + 提交**

README 补加固说明（限流/成本/快路径/管理令牌）。提交：
```bash
git add web/chat.html README.md
git commit -m "feat(harden): 前端管理令牌透传 + README 加固说明"
```

---

## Self-Review（作者自查）

- **Spec 覆盖**：对应设计文档第 5 节 W3.5「鉴权 + 限流 + 成本上限 + 规则/LLM 混合路由」。混合路由以"规则快路径（命中秒回）+ 未命中走完整 Agent"落地，符合规则优先 + LLM 兜底。✅
- **不动核心**：加固仅在 `app/hardening/` 与服务层；`chat.py` 零改动。✅
- **不破坏既有**：`/api/chat` 前置检查都短路为标准 SSE；管理端点鉴权默认空令牌=放行，既有测试不受影响。✅
- **本地友好**：默认 `admin_token=""`、宽松阈值；本地体验零摩擦。✅
- **类型一致性**：`RateLimiter.allow`/`CostGuard.allow/spent`/`match_fast_path`/`make_admin_auth` 在 app.py 调用签名一致；短路响应统一 `reply`+`done`。✅

---

## 完成即达成的里程碑

服务具备公网上线的基本防护：会话限流防刷、每日预算防烧钱、高频寒暄规则秒回省成本、管理接口令牌保护。至此项目从"能演示"迈向"敢公网上线"。下一步 **W4**：评估回归门禁 + Trace 回流 + 容器化部署（Docker/compose）+ 收尾。
```
