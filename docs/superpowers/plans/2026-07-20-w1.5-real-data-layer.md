# W1.5：真实数据层（SQLite）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把写死的 `app/agent/tools/mock_data.py` 替换为一个真实的 SQLite 数据层（商品/订单/物流/用户），并把 5 个电商工具改为查真库；数据能被后台脚本改动、且对话结果随之变化——完成"从假数据到真数据"的关键一步。

**Architecture:** 新增 `app/db/` 数据层：`Database` 类封装连接与建表，`seed.py` 把现有 mock 内容灌入库（复用 `mock_data.py` 作为唯一种子来源，DRY）。数据访问以 `Database` 的方法提供，**返回结构与旧 mock 工具完全一致**，因此 5 个工具只换数据源、不改返回契约，Agent/prompt 行为零扰动。工具通过 `get_db()`/`set_db()` 单例访问器拿到 `Database`（沿用本仓库既有的 `set_memory_manager`/`set_skill_manager` 依赖注入范式），测试用临时库注入实现隔离。

**Tech Stack:** Python 3.11+、标准库 `sqlite3`（`row_factory=sqlite3.Row`）、pytest（临时库离线测试，不依赖 API）。

## Global Constraints

- **Python 3.11+**；解释器用 venv：`D:/2026项目/ecom-service-agent/.venv/Scripts/python.exe`。
- **保持工具返回契约不变**：`query_order` / `query_product` / `query_logistics` / `list_user_orders` 对已有种子数据的返回结构，必须与改造前一致（键名、嵌套结构；允许多出值为 `None` 的显式列）。
- **唯一有意的行为升级**：`apply_refund` 由"仅返回话术"升级为"**真正修改订单状态入库**"（这是"真实数据流动"的核心演示点，需在报告中显式说明）。
- **测试离线**：新增测试一律用 `tmp_path` 建临时库并 seed，**不得**依赖真实 DB 文件、`OPENAI_API_KEY` 或网络。
- **复用 mock_data.py 作为种子来源**：不得把订单/商品/物流数据在 `seed.py` 里重新抄一遍。
- **Windows 友好**：路径用 `pathlib`。
- **DB 路径来自 settings**：`settings.db_path`，默认 `app/sessions/ecom.db`（`app/sessions/` 已被 .gitignore，不入库）。
- **`sqlite3.Row`**：连接统一设 `row_factory = sqlite3.Row`，便于 `dict(row)`。

---

## File Structure

- `app/config/settings.py` — 修改：新增 `db_path`。
- `app/db/__init__.py` — 新建：导出 `Database` / `get_db` / `set_db`。
- `app/db/database.py` — 新建：`Database` 类（连接 + 建表 + 读写方法）。
- `app/db/seed.py` — 新建：`seed_from_mock(db)` 把 mock 内容灌入库。
- `app/scripts/init_db.py` — 新建：一键建库 + 灌种子（`python -m app.scripts.init_db`）。
- `app/scripts/admin.py` — 新建：后台改数据 CLI（改订单状态 / 改库存）。
- `app/agent/tools/order.py` / `product.py` / `logistics.py` / `refund.py` / `user_orders.py` — 修改：数据源从 `mock_data` 改为 `get_db()`。
- `tests/test_db_schema.py` / `tests/test_db_repository.py` / `tests/test_db_write.py` / `tests/test_tools_db.py` — 新建：分层测试。

> `app/agent/tools/mock_data.py` **保留**（作为种子来源），不删除。

---

## Task 1: 配置项 + Database 类（连接与建表）

新增 `app/db/` 包与 `Database` 类，负责 SQLite 连接与建表，并提供 `get_db()`/`set_db()` 单例访问器。

**Files:**
- Modify: `app/config/settings.py`（在"API 服务"配置块附近加 `db_path`）
- Create: `app/db/__init__.py`
- Create: `app/db/database.py`
- Test: `tests/test_db_schema.py`

**Interfaces:**
- Produces:
  - `settings.db_path: str`（默认 `"app/sessions/ecom.db"`）
  - `app/db/database.py`：`class Database`
    - `__init__(self, db_path: Optional[str] = None)` —— 缺省用 `settings.db_path`
    - `connect(self) -> sqlite3.Connection` —— 已设 `row_factory=sqlite3.Row`
    - `init_schema(self) -> None` —— 建 6 张表（IF NOT EXISTS）
  - `app/db/__init__.py`：`Database`、`get_db() -> Database`、`set_db(db: Database) -> None`

- [ ] **Step 1: 写失败测试**

`tests/test_db_schema.py`：
```python
from app.db import Database, get_db, set_db


def test_init_schema_creates_all_tables(tmp_path):
    db = Database(str(tmp_path / "t.db"))
    db.init_schema()
    conn = db.connect()
    try:
        rows = conn.execute(
            "SELECT name FROM sqlite_master WHERE type='table'"
        ).fetchall()
        names = {r["name"] for r in rows}
    finally:
        conn.close()
    assert {"products", "orders", "order_items",
            "shipments", "logistics_events", "users"} <= names


def test_connect_uses_row_factory(tmp_path):
    db = Database(str(tmp_path / "t.db"))
    db.init_schema()
    conn = db.connect()
    try:
        row = conn.execute("SELECT 1 AS x").fetchone()
        assert row["x"] == 1  # Row 支持按列名取值
    finally:
        conn.close()


def test_get_set_db_singleton(tmp_path):
    db = Database(str(tmp_path / "t.db"))
    set_db(db)
    assert get_db() is db
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd "D:/2026项目/ecom-service-agent" && .venv/Scripts/python.exe -m pytest tests/test_db_schema.py -v`
Expected: FAIL —— `ModuleNotFoundError: No module named 'app.db'`。

- [ ] **Step 3: 实现**

在 `app/config/settings.py` 的 `api_port` 行下方加：
```python
    # 真实数据层（W1.5）
    db_path: str = "app/sessions/ecom.db"
```
新建 `app/db/database.py`：
```python
"""真实数据层：SQLite 连接、建表与读写。"""

import json
import sqlite3
from datetime import datetime
from pathlib import Path
from typing import Optional

from app.config.settings import settings


class Database:
    def __init__(self, db_path: Optional[str] = None):
        self.db_path = db_path or settings.db_path

    def connect(self) -> sqlite3.Connection:
        Path(self.db_path).parent.mkdir(parents=True, exist_ok=True)
        conn = sqlite3.connect(self.db_path)
        conn.row_factory = sqlite3.Row
        return conn

    def init_schema(self) -> None:
        conn = self.connect()
        try:
            conn.executescript(
                """
                CREATE TABLE IF NOT EXISTS products (
                    product_id TEXT PRIMARY KEY,
                    name TEXT NOT NULL,
                    category TEXT,
                    price REAL,
                    stock INTEGER,
                    description TEXT,
                    specs TEXT
                );
                CREATE TABLE IF NOT EXISTS orders (
                    order_id TEXT PRIMARY KEY,
                    user TEXT,
                    status TEXT,
                    total REAL,
                    created_at TEXT,
                    shipped_at TEXT,
                    tracking_number TEXT,
                    carrier TEXT,
                    estimated_delivery TEXT,
                    delivered_at TEXT,
                    refund_reason TEXT,
                    refund_status TEXT,
                    refund_requested_at TEXT
                );
                CREATE TABLE IF NOT EXISTS order_items (
                    id INTEGER PRIMARY KEY AUTOINCREMENT,
                    order_id TEXT NOT NULL,
                    name TEXT,
                    sku TEXT,
                    quantity INTEGER,
                    price REAL
                );
                CREATE TABLE IF NOT EXISTS shipments (
                    tracking_number TEXT PRIMARY KEY,
                    carrier TEXT,
                    status TEXT
                );
                CREATE TABLE IF NOT EXISTS logistics_events (
                    id INTEGER PRIMARY KEY AUTOINCREMENT,
                    tracking_number TEXT NOT NULL,
                    seq INTEGER,
                    time TEXT,
                    location TEXT,
                    description TEXT
                );
                CREATE TABLE IF NOT EXISTS users (
                    user_id TEXT PRIMARY KEY,
                    name TEXT
                );
                CREATE INDEX IF NOT EXISTS idx_items_order ON order_items(order_id);
                CREATE INDEX IF NOT EXISTS idx_events_tn ON logistics_events(tracking_number);
                """
            )
            conn.commit()
        finally:
            conn.close()
```
新建 `app/db/__init__.py`：
```python
from app.db.database import Database

_DB: Database | None = None


def get_db() -> Database:
    global _DB
    if _DB is None:
        _DB = Database()
    return _DB


def set_db(db: Database) -> None:
    global _DB
    _DB = db


__all__ = ["Database", "get_db", "set_db"]
```

- [ ] **Step 4: 运行测试确认通过**

Run: `cd "D:/2026项目/ecom-service-agent" && .venv/Scripts/python.exe -m pytest tests/test_db_schema.py -v`
Expected: PASS（3 passed）。

- [ ] **Step 5: 提交**

```bash
git add app/config/settings.py app/db/__init__.py app/db/database.py tests/test_db_schema.py
git commit -m "feat(db): 新增 SQLite 数据层 Database 类与建表"
```

---

## Task 2: 种子数据（把 mock 灌入库）

新增 `seed_from_mock(db)`：复用 `mock_data.py` 的 `ORDERS/PRODUCTS/LOGISTICS`，写入 6 张表。

**Files:**
- Create: `app/db/seed.py`
- Test: `tests/test_db_schema.py`（追加 seed 断言）

**Interfaces:**
- Consumes: `Database`（Task 1）、`app/agent/tools/mock_data.py` 的 `ORDERS/PRODUCTS/LOGISTICS`。
- Produces: `app/db/seed.py`：`seed_from_mock(db: Database) -> None`

- [ ] **Step 1: 写失败测试**

在 `tests/test_db_schema.py` 末尾追加：
```python
def test_seed_from_mock_populates_tables(tmp_path):
    from app.db.seed import seed_from_mock
    from app.agent.tools.mock_data import ORDERS, PRODUCTS, LOGISTICS

    db = Database(str(tmp_path / "t.db"))
    db.init_schema()
    seed_from_mock(db)

    conn = db.connect()
    try:
        n_orders = conn.execute("SELECT COUNT(*) AS c FROM orders").fetchone()["c"]
        n_products = conn.execute("SELECT COUNT(*) AS c FROM products").fetchone()["c"]
        n_events = conn.execute("SELECT COUNT(*) AS c FROM logistics_events").fetchone()["c"]
        n_items = conn.execute("SELECT COUNT(*) AS c FROM order_items").fetchone()["c"]
    finally:
        conn.close()

    assert n_orders == len(ORDERS)
    assert n_products == len(PRODUCTS)
    assert n_events == sum(len(v["events"]) for v in LOGISTICS.values())
    assert n_items == sum(len(o["items"]) for o in ORDERS.values())
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd "D:/2026项目/ecom-service-agent" && .venv/Scripts/python.exe -m pytest tests/test_db_schema.py::test_seed_from_mock_populates_tables -v`
Expected: FAIL —— `ModuleNotFoundError: No module named 'app.db.seed'`。

- [ ] **Step 3: 实现**

新建 `app/db/seed.py`：
```python
"""把 mock_data 的内容灌入 SQLite（唯一种子来源，DRY）。"""

import json

from app.db.database import Database


def seed_from_mock(db: Database) -> None:
    from app.agent.tools.mock_data import ORDERS, PRODUCTS, LOGISTICS

    conn = db.connect()
    try:
        for p in PRODUCTS.values():
            conn.execute(
                """INSERT OR REPLACE INTO products
                   (product_id, name, category, price, stock, description, specs)
                   VALUES (?, ?, ?, ?, ?, ?, ?)""",
                (p["product_id"], p["name"], p.get("category"), p.get("price"),
                 p.get("stock"), p.get("description", ""),
                 json.dumps(p.get("specs", {}), ensure_ascii=False)),
            )

        users = {}
        for o in ORDERS.values():
            conn.execute(
                """INSERT OR REPLACE INTO orders
                   (order_id, user, status, total, created_at, shipped_at,
                    tracking_number, carrier, estimated_delivery, delivered_at,
                    refund_reason, refund_status, refund_requested_at)
                   VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)""",
                (o["order_id"], o.get("user"), o.get("status"), o.get("total"),
                 o.get("created_at"), o.get("shipped_at"), o.get("tracking_number"),
                 o.get("carrier"), o.get("estimated_delivery"), o.get("delivered_at"),
                 o.get("refund_reason"), o.get("refund_status"),
                 o.get("refund_requested_at")),
            )
            for it in o["items"]:
                conn.execute(
                    """INSERT INTO order_items (order_id, name, sku, quantity, price)
                       VALUES (?, ?, ?, ?, ?)""",
                    (o["order_id"], it["name"], it["sku"], it["quantity"], it["price"]),
                )
            if o.get("user"):
                users[o["user"]] = o["user"]

        for name in users:
            conn.execute(
                "INSERT OR REPLACE INTO users (user_id, name) VALUES (?, ?)",
                (name, name),
            )

        for lg in LOGISTICS.values():
            conn.execute(
                "INSERT OR REPLACE INTO shipments (tracking_number, carrier, status) VALUES (?, ?, ?)",
                (lg["tracking_number"], lg["carrier"], lg["status"]),
            )
            for seq, ev in enumerate(lg["events"]):
                conn.execute(
                    """INSERT INTO logistics_events
                       (tracking_number, seq, time, location, description)
                       VALUES (?, ?, ?, ?, ?)""",
                    (lg["tracking_number"], seq, ev["time"], ev["location"], ev["description"]),
                )
        conn.commit()
    finally:
        conn.close()
```

- [ ] **Step 4: 运行测试确认通过**

Run: `cd "D:/2026项目/ecom-service-agent" && .venv/Scripts/python.exe -m pytest tests/test_db_schema.py -v`
Expected: PASS（4 passed）。

- [ ] **Step 5: 提交**

```bash
git add app/db/seed.py tests/test_db_schema.py
git commit -m "feat(db): 种子数据 seed_from_mock（复用 mock_data）"
```

---

## Task 3: 读方法（保持工具返回契约）

在 `Database` 上实现读方法，返回结构与旧 mock 一致。

**Files:**
- Modify: `app/db/database.py`（新增读方法）
- Test: `tests/test_db_repository.py`

**Interfaces:**
- Produces（`Database` 方法）：
  - `get_order(order_id: str) -> Optional[dict]` —— 含 `items` 列表；无则 `None`
  - `list_orders() -> list[dict]` —— 每项为完整 order dict（含 items）
  - `get_product(product_id: str) -> Optional[dict]` —— `specs` 为 dict
  - `all_products() -> list[dict]` —— 每项 `specs` 为 dict
  - `get_logistics(tracking_number: str) -> Optional[dict]` —— `{tracking_number, carrier, status, events:[{time,location,description}]}`

- [ ] **Step 1: 写失败测试**

`tests/test_db_repository.py`：
```python
import pytest

from app.db import Database
from app.db.seed import seed_from_mock


@pytest.fixture
def db(tmp_path):
    d = Database(str(tmp_path / "t.db"))
    d.init_schema()
    seed_from_mock(d)
    return d


def test_get_order_shape(db):
    order = db.get_order("ORD-20240115-001")
    assert order["order_id"] == "ORD-20240115-001"
    assert order["status"] == "shipped"
    assert order["tracking_number"] == "SF1234567890"
    assert isinstance(order["items"], list) and len(order["items"]) == 1
    assert order["items"][0]["sku"] == "SHOE-270-BK-42"


def test_get_order_missing_returns_none(db):
    assert db.get_order("NOPE") is None


def test_list_orders_count(db):
    from app.agent.tools.mock_data import ORDERS
    assert len(db.list_orders()) == len(ORDERS)


def test_get_product_specs_is_dict(db):
    p = db.get_product("ELEC-APP-002")
    assert p["name"] == "Apple AirPods Pro 2"
    assert isinstance(p["specs"], dict)
    assert p["specs"]["颜色"] == "白色"


def test_all_products_count(db):
    from app.agent.tools.mock_data import PRODUCTS
    all_p = db.all_products()
    assert len(all_p) == len(PRODUCTS)
    assert all(isinstance(p["specs"], dict) for p in all_p)


def test_get_logistics_shape(db):
    lg = db.get_logistics("SF1234567890")
    assert lg["carrier"] == "顺丰速运"
    assert lg["status"] == "in_transit"
    assert len(lg["events"]) == 4
    assert lg["events"][0]["description"] == "快件已揽收"


def test_get_logistics_missing_returns_none(db):
    assert db.get_logistics("NOPE") is None
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd "D:/2026项目/ecom-service-agent" && .venv/Scripts/python.exe -m pytest tests/test_db_repository.py -v`
Expected: FAIL —— `AttributeError: 'Database' object has no attribute 'get_order'`。

- [ ] **Step 3: 实现**

在 `app/db/database.py` 的 `Database` 类中，`init_schema` 之后追加：
```python
    # ---------- 读方法 ----------
    def _order_from_row(self, conn, row) -> dict:
        order = dict(row)
        items = conn.execute(
            "SELECT name, sku, quantity, price FROM order_items WHERE order_id = ? ORDER BY id",
            (order["order_id"],),
        ).fetchall()
        order["items"] = [dict(i) for i in items]
        return order

    def get_order(self, order_id: str) -> Optional[dict]:
        conn = self.connect()
        try:
            row = conn.execute(
                "SELECT * FROM orders WHERE order_id = ?", (order_id,)
            ).fetchone()
            return self._order_from_row(conn, row) if row else None
        finally:
            conn.close()

    def list_orders(self) -> list[dict]:
        conn = self.connect()
        try:
            rows = conn.execute("SELECT * FROM orders ORDER BY created_at").fetchall()
            return [self._order_from_row(conn, r) for r in rows]
        finally:
            conn.close()

    @staticmethod
    def _product_from_row(row) -> dict:
        p = dict(row)
        p["specs"] = json.loads(p["specs"]) if p.get("specs") else {}
        return p

    def get_product(self, product_id: str) -> Optional[dict]:
        conn = self.connect()
        try:
            row = conn.execute(
                "SELECT * FROM products WHERE product_id = ?", (product_id,)
            ).fetchone()
            return self._product_from_row(row) if row else None
        finally:
            conn.close()

    def all_products(self) -> list[dict]:
        conn = self.connect()
        try:
            rows = conn.execute("SELECT * FROM products").fetchall()
            return [self._product_from_row(r) for r in rows]
        finally:
            conn.close()

    def get_logistics(self, tracking_number: str) -> Optional[dict]:
        conn = self.connect()
        try:
            ship = conn.execute(
                "SELECT * FROM shipments WHERE tracking_number = ?", (tracking_number,)
            ).fetchone()
            if not ship:
                return None
            events = conn.execute(
                "SELECT time, location, description FROM logistics_events "
                "WHERE tracking_number = ? ORDER BY seq",
                (tracking_number,),
            ).fetchall()
            return {
                "tracking_number": ship["tracking_number"],
                "carrier": ship["carrier"],
                "status": ship["status"],
                "events": [dict(e) for e in events],
            }
        finally:
            conn.close()
```

- [ ] **Step 4: 运行测试确认通过**

Run: `cd "D:/2026项目/ecom-service-agent" && .venv/Scripts/python.exe -m pytest tests/test_db_repository.py -v`
Expected: PASS（7 passed）。

- [ ] **Step 5: 提交**

```bash
git add app/db/database.py tests/test_db_repository.py
git commit -m "feat(db): Database 读方法（订单/商品/物流，保持工具契约）"
```

---

## Task 4: 写方法（退款改库 / 改状态 / 改库存）

实现会真正改动数据的写方法，为 `apply_refund` 升级和后台 admin 服务。

**Files:**
- Modify: `app/db/database.py`（新增写方法）
- Test: `tests/test_db_write.py`

**Interfaces:**
- Produces（`Database` 方法）：
  - `set_refund(order_id: str, reason: str) -> bool` —— 置 `status='refund_processing'`、`refund_status='审核中'`、`refund_reason`、`refund_requested_at=now`；成功返回 True，订单不存在返回 False
  - `update_order_status(order_id: str, status: str) -> bool`
  - `update_stock(product_id: str, stock: int) -> bool`

- [ ] **Step 1: 写失败测试**

`tests/test_db_write.py`：
```python
import pytest

from app.db import Database
from app.db.seed import seed_from_mock


@pytest.fixture
def db(tmp_path):
    d = Database(str(tmp_path / "t.db"))
    d.init_schema()
    seed_from_mock(d)
    return d


def test_set_refund_mutates_order(db):
    ok = db.set_refund("ORD-20240115-001", "尺码不合适")
    assert ok is True
    order = db.get_order("ORD-20240115-001")
    assert order["status"] == "refund_processing"
    assert order["refund_status"] == "审核中"
    assert order["refund_reason"] == "尺码不合适"
    assert order["refund_requested_at"]  # 非空


def test_set_refund_missing_returns_false(db):
    assert db.set_refund("NOPE", "x") is False


def test_update_order_status(db):
    assert db.update_order_status("ORD-20240120-002", "shipped") is True
    assert db.get_order("ORD-20240120-002")["status"] == "shipped"


def test_update_stock(db):
    assert db.update_stock("ELEC-APP-002", 5) is True
    assert db.get_product("ELEC-APP-002")["stock"] == 5


def test_update_stock_missing_returns_false(db):
    assert db.update_stock("NOPE", 1) is False
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd "D:/2026项目/ecom-service-agent" && .venv/Scripts/python.exe -m pytest tests/test_db_write.py -v`
Expected: FAIL —— `AttributeError: 'Database' object has no attribute 'set_refund'`。

- [ ] **Step 3: 实现**

在 `app/db/database.py` 的 `Database` 类末尾追加：
```python
    # ---------- 写方法 ----------
    def _now(self) -> str:
        return datetime.now().strftime("%Y-%m-%d %H:%M:%S")

    def set_refund(self, order_id: str, reason: str) -> bool:
        conn = self.connect()
        try:
            cur = conn.execute(
                """UPDATE orders
                   SET status = 'refund_processing', refund_status = '审核中',
                       refund_reason = ?, refund_requested_at = ?
                   WHERE order_id = ?""",
                (reason, self._now(), order_id),
            )
            conn.commit()
            return cur.rowcount > 0
        finally:
            conn.close()

    def update_order_status(self, order_id: str, status: str) -> bool:
        conn = self.connect()
        try:
            cur = conn.execute(
                "UPDATE orders SET status = ? WHERE order_id = ?", (status, order_id)
            )
            conn.commit()
            return cur.rowcount > 0
        finally:
            conn.close()

    def update_stock(self, product_id: str, stock: int) -> bool:
        conn = self.connect()
        try:
            cur = conn.execute(
                "UPDATE products SET stock = ? WHERE product_id = ?", (stock, product_id)
            )
            conn.commit()
            return cur.rowcount > 0
        finally:
            conn.close()
```

- [ ] **Step 4: 运行测试确认通过**

Run: `cd "D:/2026项目/ecom-service-agent" && .venv/Scripts/python.exe -m pytest tests/test_db_write.py -v`
Expected: PASS（5 passed）。

- [ ] **Step 5: 提交**

```bash
git add app/db/database.py tests/test_db_write.py
git commit -m "feat(db): Database 写方法（退款改库/改状态/改库存）"
```

---

## Task 5: 5 个工具改为查真库

把工具的数据源从 `mock_data` 换成 `get_db()`，保持返回契约；`apply_refund` 升级为真正改库。

**Files:**
- Modify: `app/agent/tools/order.py`
- Modify: `app/agent/tools/product.py`
- Modify: `app/agent/tools/logistics.py`
- Modify: `app/agent/tools/refund.py`
- Modify: `app/agent/tools/user_orders.py`
- Test: `tests/test_tools_db.py`

**Interfaces:**
- Consumes: `get_db()`/`set_db()`（Task 1）、`Database` 读写方法（Task 3/4）。
- Produces: 工具函数签名与返回结构不变（见各函数）；`apply_refund` 成功分支现在会改库。

- [ ] **Step 1: 写失败测试**

`tests/test_tools_db.py`：
```python
import pytest

from app.db import Database, set_db
from app.db.seed import seed_from_mock


@pytest.fixture(autouse=True)
def _use_temp_db(tmp_path):
    d = Database(str(tmp_path / "t.db"))
    d.init_schema()
    seed_from_mock(d)
    set_db(d)
    return d


def test_query_order_hits_db():
    from app.agent.tools.order import query_order
    r = query_order("ORD-20240115-001")
    assert r["success"] is True
    assert r["order"]["items"][0]["sku"] == "SHOE-270-BK-42"


def test_query_order_missing():
    from app.agent.tools.order import query_order
    r = query_order("NOPE")
    assert r["success"] is False


def test_query_product_by_id():
    from app.agent.tools.product import query_product
    r = query_product("ELEC-APP-002")
    assert r["success"] is True
    assert r["products"][0]["name"] == "Apple AirPods Pro 2"


def test_query_product_by_keyword():
    from app.agent.tools.product import query_product
    r = query_product("运动鞋")
    assert r["success"] is True
    assert any("运动鞋" in p["category"] or "运动鞋" in p["name"] for p in r["products"])


def test_query_logistics():
    from app.agent.tools.logistics import query_logistics
    r = query_logistics("ORD-20240115-001")
    assert r["success"] is True
    assert r["logistics"]["carrier"] == "顺丰速运"


def test_list_user_orders():
    from app.agent.tools.user_orders import list_user_orders
    from app.agent.tools.mock_data import ORDERS
    r = list_user_orders()
    assert r["success"] is True
    assert r["count"] == len(ORDERS)


def test_apply_refund_mutates_db():
    from app.agent.tools.refund import apply_refund
    from app.agent.tools.order import query_order
    r = apply_refund("ORD-20240115-001", "尺码不合适")
    assert r["success"] is True
    # 退款后再查订单，状态已变（真实数据流动）
    assert query_order("ORD-20240115-001")["order"]["status"] == "refund_processing"


def test_apply_refund_already_processing():
    from app.agent.tools.refund import apply_refund
    r = apply_refund("ORD-20240118-004", "x")  # 该订单初始即 refund_processing
    assert r["success"] is False
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd "D:/2026项目/ecom-service-agent" && .venv/Scripts/python.exe -m pytest tests/test_tools_db.py -v`
Expected: FAIL —— 现有工具仍从 `mock_data` 读，`set_db` 的临时库不生效；`test_apply_refund_mutates_db` 断言状态变化会失败（旧 refund 不改库）。

- [ ] **Step 3: 实现**

`app/agent/tools/order.py` 全文替换为：
```python
from app.db import get_db


def query_order(order_id: str) -> dict:
    """根据订单号查询订单详情，包括状态、商品、金额、物流等信息。"""
    order = get_db().get_order(order_id)
    if not order:
        return {"success": False, "error": f"未找到订单 {order_id}，请核实订单号"}
    return {"success": True, "order": order}
```
`app/agent/tools/logistics.py` 全文替换为：
```python
from app.db import get_db


def query_logistics(order_id: str) -> dict:
    """根据订单号查询物流轨迹信息。"""
    db = get_db()
    order = db.get_order(order_id)
    if not order:
        return {"success": False, "error": f"未找到订单 {order_id}，请核实订单号"}

    tracking_number = order.get("tracking_number")
    if not tracking_number:
        return {"success": False, "error": "该订单尚未发货，暂无物流信息"}

    logistics = db.get_logistics(tracking_number)
    if not logistics:
        return {"success": False, "error": f"物流单号 {tracking_number} 暂无轨迹信息"}

    return {"success": True, "logistics": logistics}
```
`app/agent/tools/user_orders.py` 全文替换为：
```python
from app.db import get_db

STATUS_LABELS = {
    "pending": "待发货",
    "shipped": "已发货",
    "delivered": "已签收",
    "refund_processing": "退款中",
}


def list_user_orders() -> dict:
    """查询当前用户的所有订单概要列表。"""
    orders = [
        {
            "order_id": o["order_id"],
            "status": STATUS_LABELS.get(o["status"], o["status"]),
            "items_summary": "、".join(item["name"] for item in o["items"]),
            "total": o["total"],
            "created_at": o["created_at"],
        }
        for o in get_db().list_orders()
    ]
    return {"success": True, "count": len(orders), "orders": orders}
```
`app/agent/tools/product.py`：把顶部 `from app.agent.tools.mock_data import PRODUCTS` 改为 `from app.db import get_db`，并把 `query_product` 改为用 DB（保留 `_match_score` 与 `_generate_mock_product` 不变）：
```python
def query_product(keyword: str) -> dict:
    """根据商品名称关键词或商品ID查询商品信息，包括价格、库存、规格等。"""
    db = get_db()
    exact = db.get_product(keyword)
    if exact:
        return {"success": True, "products": [exact]}

    keywords = [kw.lower() for kw in keyword.split() if kw.strip()]
    if not keywords:
        keywords = [keyword.lower()]

    scored = [(p, _match_score(p, keywords)) for p in db.all_products()]
    results = [p for p, score in scored if score > 0]

    if not results:
        return {"success": True, "products": [_generate_mock_product(keyword)]}
    return {"success": True, "products": results}
```
`app/agent/tools/refund.py` 全文替换为（成功分支改库）：
```python
from app.db import get_db


def apply_refund(order_id: str, reason: str) -> dict:
    """为指定订单申请退款，需提供退款原因。"""
    db = get_db()
    order = db.get_order(order_id)
    if not order:
        return {"success": False, "error": f"未找到订单 {order_id}，请核实订单号"}

    if order["status"] == "refund_processing":
        return {"success": False, "error": "该订单已有退款申请正在处理中，请耐心等待"}

    was_pending = order["status"] == "pending"
    db.set_refund(order_id, reason)

    if was_pending:
        return {
            "success": True,
            "message": (
                f"订单 {order_id} 尚未发货，已直接取消并发起退款。"
                f"退款原因：{reason}。退款将在 1-3 个工作日内原路退回。"
            ),
        }

    return {
        "success": True,
        "message": (
            f"退款申请已提交。订单 {order_id}，退款原因：{reason}。"
            f"预计 1-3 个工作日内审核完成，届时会通知您退货地址。"
        ),
    }
```

- [ ] **Step 4: 运行测试确认通过**

Run: `cd "D:/2026项目/ecom-service-agent" && .venv/Scripts/python.exe -m pytest tests/test_tools_db.py -v`
Expected: PASS（8 passed）。

- [ ] **Step 5: 提交**

```bash
git add app/agent/tools/order.py app/agent/tools/product.py app/agent/tools/logistics.py app/agent/tools/refund.py app/agent/tools/user_orders.py tests/test_tools_db.py
git commit -m "feat(tools): 5 个工具改为查真实 SQLite 库（refund 升级为改库）"
```

---

## Task 6: 建库脚本 + 后台 admin CLI + 端到端

提供一键建库脚本与后台改数据 CLI，完成"改数据 → 对话结果变化"的端到端验证。

**Files:**
- Create: `app/scripts/init_db.py`
- Create: `app/scripts/admin.py`
- Modify: `README.md`（快速开始加"建库"步骤）
- Test: 全量新增测试回归 + 人工端到端

**Interfaces:**
- Consumes: `Database`、`seed_from_mock`、`Database.update_order_status/update_stock`。
- Produces:
  - `python -m app.scripts.init_db` —— 建库 + 灌种子（若库已存在则先删除重建）
  - `python -m app.scripts.admin set-status <order_id> <status>` / `python -m app.scripts.admin set-stock <product_id> <stock>`

- [ ] **Step 1: 写脚本**

`app/scripts/init_db.py`：
```python
"""一键建库 + 灌种子：python -m app.scripts.init_db"""

from pathlib import Path

from app.config.settings import settings
from app.db import Database
from app.db.seed import seed_from_mock


def main():
    db_file = Path(settings.db_path)
    if db_file.exists():
        db_file.unlink()
        print(f"已删除旧库: {db_file}")
    db = Database(settings.db_path)
    db.init_schema()
    seed_from_mock(db)
    print(f"建库完成并已灌入种子数据: {db_file}")


if __name__ == "__main__":
    main()
```
`app/scripts/admin.py`：
```python
"""后台改数据 CLI：
    python -m app.scripts.admin set-status <order_id> <status>
    python -m app.scripts.admin set-stock <product_id> <stock>
"""

import sys

from app.db import get_db


def main(argv=None):
    argv = argv if argv is not None else sys.argv[1:]
    if len(argv) != 3:
        print(__doc__)
        return 1

    cmd, key, value = argv
    db = get_db()
    if cmd == "set-status":
        ok = db.update_order_status(key, value)
        print(f"{'成功' if ok else '失败(订单不存在)'}: 订单 {key} 状态 → {value}")
    elif cmd == "set-stock":
        ok = db.update_stock(key, int(value))
        print(f"{'成功' if ok else '失败(商品不存在)'}: 商品 {key} 库存 → {value}")
    else:
        print(__doc__)
        return 1
    return 0 if ok else 2


if __name__ == "__main__":
    sys.exit(main())
```

- [ ] **Step 2: 全量新增测试回归**

Run: `cd "D:/2026项目/ecom-service-agent" && .venv/Scripts/python.exe -m pytest tests/test_db_schema.py tests/test_db_repository.py tests/test_db_write.py tests/test_tools_db.py -v`
Expected: 全绿（4 + 7 + 5 + 8 = 24 passed）。

- [ ] **Step 3: 建库并端到端验证（真实改数据 → 对话结果变化）**

```bash
cd "D:/2026项目/ecom-service-agent"
.venv/Scripts/python.exe -m app.scripts.init_db
# 查一个订单初始状态
.venv/Scripts/python.exe -c "from app.db import get_db; print(get_db().get_order('ORD-20240120-002')['status'])"
# 后台改状态
.venv/Scripts/python.exe -m app.scripts.admin set-status ORD-20240120-002 shipped
# 再查，应变为 shipped
.venv/Scripts/python.exe -c "from app.db import get_db; print(get_db().get_order('ORD-20240120-002')['status'])"
```
Expected: 第一次打印 `pending`，改后打印 `shipped`。

- [ ] **Step 4: Web 端到端（可选，需 API Key）**

```bash
.venv/Scripts/python.exe run_api.py
```
浏览器打开 `http://127.0.0.1:8010/`，问"我的订单 ORD-20240120-002 发货了吗"，应基于真库回答；用 admin 改状态后再问，回答随之变化。

- [ ] **Step 5: 更新 README 快速开始**

在 `README.md` 的 `build_kb_index` 步骤附近加：
```markdown
# 4.5 构建真实业务数据库（订单/商品/物流，首次运行一次）
python -m app.scripts.init_db
```

- [ ] **Step 6: 提交**

```bash
git add app/scripts/init_db.py app/scripts/admin.py README.md
git commit -m "feat(db): 建库脚本 init_db + 后台改数据 admin CLI + README"
```

---

## Self-Review（作者自查）

- **Spec 覆盖**：对应设计文档第 5 节 W1.5「真实数据层：SQLite 建库 + 种子 + `mock_data.py` 工具改为查真库 + 一个后台改数据的入口」，以及第 6 节验收项"Agent 查询来自真实 SQLite 库，后台改数据后对话结果随之变化"。全部覆盖。✅
- **占位符扫描**：无 TBD/TODO；每个代码步骤含完整代码。✅
- **类型一致性**：`Database` 读方法（Task 3）/写方法（Task 4）/`get_db·set_db`（Task 1）在工具（Task 5）与脚本（Task 6）中被调用签名一致；工具返回契约与旧 mock 逐一对齐（`query_order`→`{success, order}`、`query_product`→`{success, products}`、`query_logistics`→`{success, logistics}`、`list_user_orders`→`{success, count, orders}`）。✅
- **有意行为变更已标注**：`apply_refund` 升级为改库，已在 Global Constraints 与 Task 5 显式说明并有测试覆盖。✅
- **YAGNI**：`users` 表最小化（仅为呼应"用户库"目标，暂无工具依赖，供后续鉴权/会话使用）；保留 `product.py` 原有兜底逻辑，避免超范围改动推荐行为。✅

---

## 完成即达成的里程碑

`python -m app.scripts.init_db` 建出真实业务库，Agent 查的订单/商品/物流全部来自 SQLite，`admin` 改数据后对话结果随之变化，`apply_refund` 真正改库——项目从"mock 假数据"迈入"真实数据在流动"，为 W2 可观测性（观测真实工具调用）、W3 护栏（校验回答是否有真实数据支撑）打下地基。
```
