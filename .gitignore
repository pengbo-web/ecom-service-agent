# Python
__pycache__/
*.py[cod]
.pytest_cache/
*.egg-info/
dist/
build/
.eggs/
# 前端构建产物需入库（拉下来即可跑，无需 npm build）
!web/dist/
!web/dist/**

# 环境变量（含密钥，禁止提交）
.env

# Claude Code
.claude/

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# 虚拟环境
venv/
.venv/
env/

# 会话持久化文件
app/sessions/

# 一键启动运行时日志/PID(非项目产物)
.run/

# 执行过程草稿（SDD 进度 ledger，非项目产物）
.superpowers/

# WebUI
webui/node_modules

# Skill 自进化的运行期产物:候选/归档/搬运暂存区。
# 它们由合成器、上传端点、转正与看门狗在运行时生成,内容随真实流量变化,
# 不是源码——进版本库只会让每次跑一遍就产生一堆无意义 diff。
app/agent/skills/definitions/_candidates/
app/agent/skills/definitions/_archive/
app/agent/skills/definitions/_swap/
